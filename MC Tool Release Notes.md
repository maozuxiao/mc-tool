# MC Tool Release Notes

| 版本 | buildVersion | 说明 |
|------|--------------|------|
| 1.0.1 | 1.0.1.0 | 基线版本（含 Help 菜单、版本查询、检查更新） |
| 1.0.2 | 1.0.2.0 | 正式发布（Select 宽度 70px、头部 time 组件修复） |
| 1.0.3 | 1.0.3.0 | 更新进度优化、下载可视化、error channel 修复、非静默安装向导、头部文字配色 |
| 1.0.4 | 1.0.4.0 | 优化了规格文件下载逻辑，优化了登录加载时间长的问题 |
| 1.0.5 | 1.0.5.0 | 修复会话失效仍免登录放行导致搜索全部失败的问题（901 时自动 SSO 刷新，失败则重新登录） |
| 1.0.6 | 1.0.6.0 | 搜索条件不再拼接料号 ITEM_NUMBER：点击「查询」仅按描述条件查询；「查料号」改为独立查询，仅按料号查询，两者不再组合 |
| 1.0.7 | 1.0.7.0 | 修复批量查询去重功能 bug：点击「去重」后序号正确递增，取消去重不再叠加行；修复第二次批量搜索后部分重复条目未消失的问题 |
| 1.0.8 | 1.0.8.0 | 修复切换搜索结果标签页（物料结果/BOM 结果/规格文件）后，展开行与「查看 BOM」「查看规格文件」按钮点击事件丢失的问题；展开状态现持久化到全局 store |
| 1.0.9 | 1.0.9.0 | 查询体验与稳定性增强：①查询结果手风琴式互斥展开（展开一行自动收起其他行）；②搜索条件输入框支持鼠标拖动选中文本；③选中文本时右键弹出复制/粘贴菜单；④新增 Git 版本管理；⑤点击标题栏图标打开 [OA](http://oa.streamax.com:8080/ruiming/mc/)； ⑥帮助菜单新增「关于」（中英双语）；⑦Ctrl+滚轮缩放、Ctrl+0 复位，缩放百分比临时显示 1 秒；⑧缩放标识移至语言切换按钮左侧；⑨登录文案「OA 手机 App」改为「钉钉手机 App」（英文 DingTalk）；⑩搜索条件仅拖拽手柄（⋮⋮ 序号）可拖动整行，输入框内正常选词；⑪优化 SSO 登录落地时长（仅触发一次握手，避免连接风暴）；⑫主窗口支持自由缩放宽高，兼容 Windows 分屏 |
| 1.0.10 | 1.0.10.0 | 新增「强制走二维码登录」策略：当同时满足①当天首次登录（以设备本地自然日 00:00 为跨天基准，lastLoginDay 与今日不同）②token 最后刷新时间与本次登录间隔 > 1 小时时，跳过 token 免密恢复/自动重试，直接展示二维码登录，避免过期/失效 token 导致的重试与体验问题；token 刷新时间随 SSO 落地持久化记录。修复①：强制二维码路径在 IAM 冷启动（长时间未登录）时二维码获取超时——step1 取登录页重试 3→5 次、单次超时 15s→12s、间隔平滑（5/8/11/14s），并新增冷启动预热（距上次成功 SSO >30 分钟先等 8s）。修复②：扫码后"进工具→转圈→回扫码"——根因是「SSO 落地判定只看 cookie 名，OA 实际返回 901 仍被误判为已登录骗进工具，首次查询被踢回扫码」。改为以真实探测（probeOaSession 200/ok）为唯一判定标准：QR-POLL 落地后先真实探测确认 OA 会话生效，且扫码路径对网络超时(-118)不兜底放行；SSO 未真正落地时主动通知前端回到扫码重试，关闭"正在进入工具"覆盖层，不再骗进工具后被 901 踢出。**1.0.10 后续修复**：①修复 SSO 落地失败时原因被覆盖的 bug（内层带 reason 的通知被外层无 reason 通知覆盖，导致前端无法显示网络提示）；②IAM 连接超时(-118)在 main-window 落地阶段被误判为 reauth，现正确识别为 network；③step1 取登录页重试策略进一步收紧（最多 4 次尝试 + 指数退避 1.5/3/4.5s，单超时 8s/5s，最坏上限 ~32s，替代原 8 轮固定 2s 重试）；④网络异常时二维码自动重新拉取并刷新显示，无需用户手动点击刷新（含 5s 间隔、最多 6 次有限自动重试，IAM 恢复后自动出码）；⑤登录会话备份/恢复链路稳定，登出后重启可免登录恢复 8 个 OA 会话 cookie |
| 1.0.11 | 1.0.11.0 | 优化「每天首次登录」冷启动二维码获取耗时：①冷启动预热由固定盲等 8s 改为**自适应探测**——循环轻量探测 OA 入口可达性（短超时 4s、每 2s 探一次、上限 40s），IAM 冷态一恢复（实测约 20~35s）立即取登录页出码，避免盲等 + 反复超时重试叠加导致的「等 1 分钟才出码」；②step1 取登录页重试兜底收紧（首超 6s、重试超 4s、退避 2/3/4/4/4s、最多 6 次），命中即出码。实测首次登录从约 60s 降至 IAM 恢复即时出码。**1.0.11 后续修复**：修复「扫码认证成功但 SSO 落地失败（reauth，IAM→OA 信任链偶发未打通）时，提示『登录未完成，请重新扫描二维码』却不再自动出现新二维码」的问题——原设计仅网络异常(network)自动重拉、reauth 停在失败态等待手动刷新，导致已扫过的废码残留且不刷新。现改为：无论 network 还是 reauth，SSO 未真正落地时均通过 qrRefetchSeq 自动重新拉取并显示新二维码（提示文案区分网络异常/登录未完成），用户无需手动点击刷新。**1.0.11 SSO 落地加速**：优化 `completeOaSso` 落地循环——①进循环前先主动触发一次 OA 业务接口握手（预触发），让 oa↔iam 静默 SSO 立即开始，避免首轮 probe 空等；②轮询间隔由 800ms 缩短至 400ms，循环上限 12→16 轮。`tryAutoSsoRefresh` 等待落地间隔同步由 1000ms 降至 400ms、上限 10→16 轮。实测扫码后 SSO 落地从约 11s（round≈11）降至约 3.8s（round=0，预触发即命中），提速近 3 倍 |
| 1.0.12 | 1.0.12.0 | UI 整理：从「帮助」下拉菜单移除「版本号」项——版本信息已包含在「关于」弹窗中，避免重复展示 |
| 1.0.13 | 1.0.13.0 | **根治「登出后二维码重载慢 + 扫码后进入工具慢」**（实测从 ~49-64s 降至 ~11s，100% 成功）。**最终确认的真实根因（多轮日志 + HAR 实证）**：① `step1` 取登录页跟随 302 进 IAM SPA 会冷场，step1 连败超时导致二维码 ~49s 才出；② `completeOaSso` 用隐藏窗口 load IAM SPA 落地页 `/ac/#/index?lck=`，由 SPA 的 JS 回跳 `oa?code=` 建 OA 会话（**回跳是 SPA JS 发出，非服务端 302，故无法用纯 HTTP 跟随绕开 SPA**）；③ ssoWin webContents **首次 `loadURL` 时 partition cookie store 未热**，首请求 `hdrLen=96`、完全不带 cookie（连 route/SESSION 都不带），导致首轮 SPA 认证必失败/静默卡死，原本要等 35s 兜底才重试。**修复**：1) `step1` 加 `stopAtLck`——命中 `ac/#/index?lck=` 这一跳立即从 Location 解析 lck 提前返回，不再 GET 冷场 SPA，登出→出码从 ~49s 降至 ~2s；2) `fireOa` 重写重试策略——区分「已 settle」与「卡死」：仅当上一轮卡死才 `stop()`+重 load，避免每轮杀掉冷场 SPA 的死循环，也不干等永不重试；单轮兜底 22s 覆盖 SPA 正常冷场峰值（~16s）；3) **首轮兜底 8s**（仅 tag=0）——首轮当作 cookie store 预热消耗，8s 判失败即 `stop`+重 load，第二轮 store 已热、SPA 1s 内回跳 `oa?code=` 落地，总耗时从 ~26-64s 降到 ~11s（2 轮）。ssoWin 改为 1×1 像素可见窗口（避免屏幕外坐标导致网络/GPU 初始化异常、SPA 静默卡死）。保留：清旧 OA/streamax cookie 防挂起、`checkOaSessionCookie` 排除 iam 域防假登录。**已知回归（已于 1.0.14 修复）**：1.0.13 将「OA 会话 cookie 名出现」当落地信号 + `isOALoggedin` 无条件信任 cookie，导致 cookie 名存在但 OA 实际 901 reauth（信任链未打通）时仍被误判为已登录放行进工具，查询被 OA 302 踢回 IAM、内部功能全废（见 2026-08-26 日志）。 |
| 1.0.14 | 1.0.14.0 | **修复 1.0.13 登录态误判回归**：当日首次打开（扫码后）cookie 已恢复但 OA 接口仍 `901 reauth`（IAM↔OA 信任链未真正打通）时，app 误判已登录放行进工具，导致所有查询被 OA 302 踢回 IAM、内部功能全部不可用。**根因**：1.0.13 过度信任 cookie——`isOALoggedin` 在 `hasOaSession=true` 时无条件 `return true` 无视 probe；`completeOaSso` 主循环把「OA 会话 cookie 名出现」当落地信号；`startup` 恢复成功后直接放行不探测。`isOALoggedin` 注释原称「浏览器登录后 probe 同样 901 但能查数据」——该假设在我们这串 cookie 场景不成立（OA 实测把带旧 cookie 的请求当未登录 302）。**修复**：以真实探测 `probeOaSession` 为**唯一权威判定**，仅在探针因「网络级错误」(reason=network，如 -118 超时/连接失败) 时才信任 cookie 兜底，避免网络抖动误踢真登录；`probe=reauth`（OA 明确拒绝）一律视为未登录并报前端重新扫码。涉及 4 处：`isOALoggedin`、`completeOaSso` 主循环、`startup` 恢复判定、`QR-POLL` 落地判定。恢复 1.0.10「以真实探测为唯一判定标准」的正确路线。**已知回归（已于 1.0.15 修复）**：1.0.14 重构 `QR-POLL` 落地判定时误删 `finalOk` 变量，导致 `if (finalOk)` 抛 ReferenceError、`OA_CHECK_LOGGED` 永不发送，扫码成功后前端卡在「进入工具」无法进入（SSO 实际已落地、OA 接口已 200，只是未通知前端）。 |
| 1.0.15 | 1.0.15.0 | **修复 1.0.14 QR-POLL 进不去工具**：1.0.14 重构 `QR-POLL` 落地判定时删除 `finalOk` 变量，导致 `if (finalOk)` 引用未定义变量抛 `ReferenceError`，被外层 catch 吞掉，`IPC.OA_CHECK_LOGGED` 永不向渲染进程发送「已登录」信号，扫码成功后界面卡在「正在进入工具」无法进入工具页面（实际 SSO 已完成、OA 接口已 200）。**修复**：恢复 `const finalOk = landed` 定义，确保登录真正落地时及时通知前端进工具；未落地时才报前端重试扫码。版本升至 1.0.15。 |
| 1.0.16 | 1.0.16.0 | **（已回退）原计划优化「扫码后进入工具」卡顿**：曾尝试把 `ssoWin` 提升模块级，并在 `OA_QR_LOGIN_START` 后台预热 iam SPA。但**实测发现致命缺陷**——预热加载的是带 `lck` 的认证 URL（`ac/#/index?lck=`），会提前消费 lck 会话状态，导致后续 `completeOaSso` 的 `fireOa` 加载同 hash 页面时 SPA 不再重新认证、永远 `reauth`（登出后彻底进不去工具，见 2026-08-26 07:07 日志）。已于 1.0.17 回退该预热逻辑。版本升至 1.0.16。 |
| 1.0.17 | 1.0.17.0 | **回退 1.0.16 的错误预热并修复「登出后无法进入工具」**：删除 `OA_QR_LOGIN_START` 中「预热 iam SPA」的后台任务（它加载带 lck 的认证 URL，提前消费 lck → `fireOa` 加载同 hash 页面时不重新认证 → 持续 `reauth` 进不去）。保留模块级 `ssoWin`/`createSsoWin` 复用（仅作为窗口复用，不主动预热），握手冷启动仍由 `fireOa` 首轮 8s 兜底处理（1.0.13/1.0.15 已验证可用）。扫描后已进入工具恢复可达。版本升至 1.0.17。 |
| 1.0.18 | 1.0.18.0 | **重构登录落地路径（初版，已修正见 1.0.19）**：曾尝试把 IAM→OA 的 SSO 落地改为纯 HTTP 链（POST authnEngine 取 `oa/?code=` 手动跟随）。但实测 iam 实际返回的是 `oa/?state=`（非 `?code=`），纯 HTTP 无法完成后续 OA code 交换（需浏览器 JS），且会多耗 ~7s 超时后才 fallback。已于 1.0.19 回退该 HTTP 路径。版本升至 1.0.18。 |
| 1.0.19 | 1.0.19.0 | **修正预热并移除无效 HTTP 路径，加速登录**：1) 恢复「扫码阶段预热隐藏窗口」但改用 `OA_LOGIN_URL`（http://oa.streamax.com:8080/，**不带本次 lck**），仅热身 iam/oa 的 TCP/TLS 连接、不消耗本次登录的 lck（1.0.16 失败正是因为预热用了「本次 lck」提前消费、后续 fireOa 不回跳）；预热让 `completeOaSso` 的 fireOa round 0 即走通，消除 round 1/2 的 `-118` 连接超时（实测各 ~21s）。2) 移除无效的 `landOaViaHttp`（`oa/?state=` 需浏览器执行，纯 HTTP 不可行，反倒多耗 ~7s 超时）；QR-POLL 恢复为 `httpJsonWithRedirect(authUrl)` 仅建立 IAM 票据，OA 会话落地仍由预热 + BrowserWindow 完成。目标登录耗时降至 ~15s 内。版本升至 1.0.19。 |
| 1.0.20 | 1.0.20.0 | **收敛「偶尔卡 60s」的取登录页超时**：step1 取 OA 登录页偶发 IAM 抖动（首跳 302 到 iam/authenticate 后，回跳 `ac/#/index?lck=` 这跳卡住导致整链超时）。优化：单次超时收紧（首轮 5s、重试 4s），退避从 `[2,3,4,4,4]s` 收紧为 `[1.5,2,3,3,3]s`，并新增「整体硬上限 30s」（Promise.race 封顶）——即便服务端持续抖动也给前端明确的 network 出口，最坏耗时从实测 ~45s 砍到 ≤30s。抖动失败后立即走前端「网络异常、自动重新拉取二维码」机制秒出码，不再无限卡住。正常路径（命中 stopAtLck）与握手耗时（~14s）不受影响。版本升至 1.0.20。 |
| 1.0.21 | 1.0.21.0 | **修复登录握手隐藏窗口偶发闪现「无框网页」**：登录 SSO 握手复用的隐藏窗口 `ssoWin` 原为 `show:true + 1×1 像素 + frame:false + 定位 (0,0)`，刻意保留可见性以规避 GPU 节流。但 SPA（`ac/#/index?lck=`）加载期间偶发被 GPU 合成放大渲染，导致桌面左上角闪现无框网页。改为 `show:false`（隐藏窗口仍正常加载并执行 SPA 的 JS 回跳 OA，握手耗时不受影响），并移除 (0,0) 可见定位；同时在 `completeOaSso` 结束后显式 `ssoWin.hide()` 双保险，确保登录完成无论如何收起窗口。版本升至 1.0.21。 |
| 1.0.22 | 1.0.22.0 | **修复次日首登并发重试 + 收紧握手空耗（基于 1.0.21 实测日志）**：1) `OA_QR_LOGIN_START` 加 in-flight 重入锁，杜绝前端「自动重拉」与「refetchSeq 触发」并发调用导致双开 QR（两个 step1 / 两个 fireOa 争抢同一 `ssoWin`，实测出现并行 QR-START、登录互相干扰、OA 握手彻底失败）；并发时第二个 invoke 直接复用进行中的结果。2) IAM 冷态预热上限 40s→70s，覆盖「次日首登 idle>1000min」时 IAM 冷得彻底、5 次探针全 timeout 的场景。3) 握手主循环轮数 12→10、后续轮兜底超时 22s→16s，把「服务端持续拒绝」时的最坏空耗从 ~180s 收至 ~170s，正常冷态慢登（多轮 reauth 后落地）不受影响。注：本次日志 OA 持续 901 主因是 IAM 冷态下 IAM↔OA 信任链未及时建立（SPA 回跳 OA 后 OA 未发起 `?code=` 交换即拒），属服务端冷态瞬态，客户端以更快失败 + 友好重试应对。版本升至 1.0.22。 |
| 1.0.23 | 1.0.23.0 | **握手失败加速 + 定位根因（基于 1.0.22/1.0.23 实测日志，网页端同账号同机正常登录，证实非网络/服务端而系 Electron 握手链路差异）**：1) **回退**上一版误用的 `authnEngine?loginToken=xxx` 入口——`loginToken` 为**一次性**核销凭证，QR-POLL 阶段已用 `httpJsonWithRedirect(authUrl)` 核销过一次（建 IAM 票据 usk），`completeOaSso` 重复核销会被 OA 视为作废 → 901；恢复为带 `lck` 的 `ac/#/index?lck=` SPA 落地页（由 SPA 的 JS 重新走授权链并 `window.location → oa?code=` 建 OA 会话，HAR 实证）。2) `fireOa` 落地判定由 `will-navigate->oa` 改为**以 `did-navigate` 最终落点为准**（停在 `oa` 业务宿主才记成功；落回 `iam` 即「OA 弹回」），更准识别握手成败。3) 握手主循环早期退出改为基于 `probe` 连续 `reauth` 计数（`consecutiveReauth>=4`）——解决「已 settled 轮不重新 load 无导航事件、原 oaRejectedCount 计数不增长」导致早期退出失效的问题；连续 4 轮 probe=reauth 即判 OA 持续拒绝、提前放弃（约 16s 而非耗满 ~170s），让前端自动重拉二维码。正常冷态慢登（历史成功日志 round 0~3 均 reauth、round 4 才成）不受影响（probe=ok 即清零）。当前状态：取码/扫码链路已稳定（约 4s 出码），但**重启 app 后偶发 OA 持续 901**仍未根治——根因指向 Electron 隐藏窗口握手与网页端在 OA 会话上下文建立上的细微差异，需进一步对比（见下次迭代）。版本升至 1.0.23。 |
| 1.0.24 | 1.0.24.0 | **修复：OA 已登录态取二维码误报错「未能从登录页获取 lck 上下文参数」**。现象：用户已登录（SESSION cookie 仍热）时访问 `oa.streamax.com:8080/` 直接返回 200、不再 302 跳转 IAM 要 lck，原 `step1` 在 `!lck` 时无条件 `throw`，把这种正常「已登录」场景当错误干掉，导致二维码拉取失败、卡在登录页。修复：`step1` 在 `lck` 为空但 `finalUrl` 已落在 `oa` 宿主且 status 2xx 时，改为调用 `probeOaSession` 真实探测——`probe.ok` 则判定「OA 已登录、无需 lck」，向前端返回 `alreadyLoggedIn:true`，前端据此跳过二维码、直接进工具；`probe.reauth` 才视为会话失效、走原重扫逻辑。该修复叠加 1.0.24 主改动（SSO 纯 HTTP 落地重写）。版本保持 1.0.24。 |
| 1.0.24 | 1.0.24.0 | **发布方式调整（私有仓库 + GitHub Releases 公开分发安装包）**：原更新源用 `generic` + 私有仓库 raw 直链，私有仓库 raw 需鉴权、协作者以外无法下载更新。现改为 `provider: 'github'`（electron-updater），安装包随 GitHub Releases 发布——**Release 附件对所有人公开可下载（无需仓库读权限/token），源码仍保持私有**。配套：`electron-builder.yml` 的 `publish` 改为 `github` provider、`buildVersion` 同步 1.0.24.0、清理不再使用的 `UPDATE_BASE_URL` 与 `feedUrl` 参数。发布流程：`pack:win` 后由 `electron-builder --publish` 自动创建 Release 上传安装包；≥1.0.24 的版本自动更新检查即生效。版本保持 1.0.24。 |
| 1.0.24 | 1.0.24.0 | **升级指引（重要）**：1.0.23 及更早版本内置的更新地址固定为旧源（`Streamax/raw/.../assets/MC_Tool`），与新 Release 地址（`maozuxiao/mc-tool` Releases）不同，**老版本无法自动检测到 1.0.24 更新**，需**手动下载覆盖安装一次**。安装 1.0.24 之后，后续版本即可正常自动更新。下载入口（公开，无需登录）：https://github.com/maozuxiao/mc-tool/releases/download/v1.0.24/mc-material-query-setup-1.0.24.exe （或前往 Releases 页面选择 `mc-material-query-setup-1.0.24.exe`）。版本保持 1.0.24。 |
| 1.0.25 | 1.0.25.0 | **仅版本号升级，无任何功能或代码改动**，用于端到端验证 GitHub Releases 自动更新链路（1.0.24 → 1.0.25）。验证要点：旧客户端启动约 3 秒后顶部更新条出现「1.0.25」与「下载」按钮；下载进度条可见；完成后点「立即更新」→ 当前 app 退出 → NSIS 安装向导非静默弹出 → 完成后自动启动新版；新版「帮助 / 检查更新」提示「当前已是最新版本」。 |
| 1.0.26 | 1.0.26.0 | **修复「隔夜/长时间未登录后首次登录」报「未能从登录页获取 lck 上下文参数」且界面卡死**。现象：启动时 `oa.host cookies=0`（OA 会话已失效，probe 901 reauth），但 `iam.streamax.com` 域仍残留 8 个陈旧 cookie（`usk`/`SESSION`/`route`/`REQID`/`LtpaToken`/`j_lang`），跳转链停在 `iam/idp/authCenter/authenticate` 直接返回 200，不再 302 到 `ac/#/index?lck=`。**根因**：这是「**IAM 半登录态**」——IAM cookie 存在但已过期，静默 SSO 被 OA 拒绝（901），IAM 又不肯对新请求发起授权流程签发 lck，于是稳定返回 200。**属确定性失败而非瞬态**：每次重发同样这批陈旧 cookie，IAM 响应必然相同——实测 7 次重试（间隔 5s）结果完全一致，故单纯重试无法自愈。代码里 `clearIamHalfLoginCookies` 本就是为此场景所写，但**只有定义、零调用点**；`OA_QR_LOGIN_START` 开头注释写明「不再主动清 IAM 半登录态，因 IAM 通常是热的」——该假设在刚登出时成立，**隔夜后失效**；登出流程 `COOKIE_CLEAR` 同样刻意保留 IAM cookie。**与 1.0.24 的关系**：1.0.24（`722e50c`）只修了「finalUrl 落在 OA 宿主且无 lck」的已登录短路分支（`onOaHost` 为 true）；本次是「finalUrl 卡在 IAM 宿主」（`onOaHost` 为 false），属同一报错文案下的**另一条未覆盖分支**，非 1.0.24 回归。**修复（两处配合）**：① 失败点（卡在 IAM 宿主）先调用 `clearIamHalfLoginCookies` 清掉陈旧 IAM 会话，让 IAM 视客户端为新会话；② 该错误标记新的 `retryable` 原因，前端 `LoginOverlay` 对 `network` 与 `retryable` 均自动退避重试（5s 间隔、最多 6 次）——于是第 1 次失败清理、第 2 次即以干净会话取码成功。仅在失败点清理，**不动「热 IAM」的正常快路径**（正常登录体验不变）；`clearIamHalfLoginCookies` 只作用于 `iam.streamax.com` 域，OA 的 `LtpaToken@.streamax.com` 不受影响。另补充清除数量与名单的日志，便于验证时确认是否真的清掉。 |
| 1.0.27 | 1.0.27.0 | **新增 AI 助手（对话页 + 内置 MC 查询工具）**。右下角新增「物料查询 / AI 助手」视图切换。① 对话能力：内置 11 个服务商预设（OpenAI / OpenCode Go / OpenCode Zen / DeepSeek / 智谱 GLM / 阿里 Qwen / Kimi / SiliconFlow / OpenRouter / Anthropic / Ollama 本地），兼容 OpenAI 与 Anthropic 两种协议；API Key 用 Electron `safeStorage` 加密存于 `userData/ai-providers.json`；支持流式输出、随时停止、Markdown（表格 + 代码高亮）渲染；会话历史持久化到 `userData/ai-history.json`。全部请求在主进程发出，API Key 不出主进程。② MC Skill：勾选后模型可调用内置 `mc_query` 工具（search / item / batch / bom / spec），复用已登录的 OA 会话无需二次扫码，气泡内可展开查看请求参数与原始返回；生命周期为退市 / 禁购 / 禁用时强制输出风险提示；规格文件只列清单，点击消息中的下载链接由应用内复用已登录 OA 会话直接下载（弹出系统保存对话框），下载与保存成功均不再弹提示，仅登录失效或网络失败时提示。Skill 随安装包分发（`resources/skills/mc-material-query-local`），首次调用自动准备 Node 22 运行时（联网下载一次）。③ 移除 `better-sqlite3`：它是原生模块，v13 要求 Node ≥ 22 而 Electron 33 内置 Node 20，且 electron-builder 会触发 node-gyp 重建（本机无 VS 生成工具会直接打包失败）；历史存储改为 JSON 文件，与项目其余持久化方式一致。④ 修复 `tsconfig.json` 的 `include` 指向不存在的目录（`electron` / `shared` / `renderer/src`，真实路径为 `src/main` / `src/preload` / `src/renderer/src`），此前 `tsc` 实际只检查了 `shared/`，历史「类型检查通过」均无效；修正后共修复 351 个类型错误，并新增 `src/renderer/src/global.d.ts`，由 preload 导出反推 `Window.mcApi` 类型，避免手写声明再次腐化。⑤ UI：底部留白统一由 `.app-root` 承担，避免固定视图切换按钮压住表格与「发送」按钮；模型下拉改为自绘弹层（原生 `<datalist>` 无法限制高度、模型多时无滚动条）；顶部视图切换按钮接入中英双语。⑥ **修复 AI 消息中规格文件下载链接点击后白屏**：Markdown 链接原由 Electron 窗口内导航到 OA 地址，而 OA 下载/登录页无法在渲染进程正常显示。现自定义 `<a>` 组件——规格文件链接走 `downloadFile` 在应用内下载，普通外部链接用系统浏览器打开；主进程再加 `will-navigate` 兜底拦截窗口内导航，彻底杜绝白屏。⑦ **修复偶发「全局无法输入、只能重启应用」**：QR-POLL 的两处异常分支吞掉异常且不发送结束事件，导致「正在进入工具…」遮罩永不解除（网络抖动才触发，故表现为偶发）。现补发 `OA_CHECK_LOGGED`，并新增 120 秒兜底自动解除遮罩，任何遗漏分支都不会再锁死界面；同时把 `window.alert / window.confirm` 全部改为走主进程 `dialog.showMessageBox`，这类同步阻塞渲染进程的弹窗同样会表现为「全局无法输入」。⑧ **会话体验**：未勾选 MC Skill 时改为普通对话模式（不再下发并不存在的工具定义，避免模型凭空编造物料数据）；首 token 之前显示「思考中…」动效；支持多会话并发生成（按会话记录生成态，切到别的会话即可继续提问，事件严格按 conversationId 过滤互不串台）；停止按钮真正生效并支持超时/已停止提示；回复语言跟随提问语言（英文提问英文回复），切换界面语言即时刷新全部文案；切换页面不再丢失当前对话，切换会话保留上次的滚动位置。⑨ 其他：消息一键复制、内置 Markdown 编辑器入口、AI 配置跨会话与跨启动持久化、OpenCode Zen 免费模型列表更新。 |
| 1.0.28 | 1.0.28.0 | **AI Build 模式增强（文件读写技能升级）**。① 版本号 1.0.27 → 1.0.28。② 新增老版 Excel（`.xls`，BIFF 二进制）读取支持（引入 SheetJS 纯 JS 库），与 `.xlsx` 一并覆盖。③ `file_search` 升级为类 FileLocatorPro 的保底检索：支持正则表达式（`--regex`）、文件名通配符（`*`/`?`）、按文件类型过滤（`--glob *.xls` / `--ext xls,xlsx`）。④ 修复「AI 工具调用轮次过多」报错：上限由 5 轮放宽到 12 轮，且达上限时强制模型基于已收集信息直接总结，不再把错误抛给用户。⑤ 同步 SKILL.md 与工具描述。⑥ 聊天消息时间戳格式由 `YYYY-MM-DD HH:MM:SS` 调整为 `YY:MM:DD HH:MM:SS`（年份缩为两位、分隔符统一为冒号）。⑦ 文件读写 Skill（Build 模式下的 `file_office` 工具）新增「整行染色」：Excel 更新 / 写入时可用 `__rowFill` / `rowFill` / `行色` / `整行颜色` / `rowColor` 键指定 `green` / `yellow` / `red` 或具体 ARGB，把该关键列所在行从 A 列到工作表最右列整体重染、覆盖旧色；键缺失时回退到该行最右侧有填充的单元格颜色。⑧ 修复 ExcelJS 串色 bug：从文件读入的既有单元格共享同一默认 style 引用，循环里直接 `cell.fill=` 会就地篡改该共享对象，导致批量整行染色时 A–D 列被最后一行颜色覆盖、全变红；改为用 `cell.style = Object.assign(...)` 给每个单元格独立 style 对象，A–D 现可正确按行着色。 |
| 1.0.29 | 1.0.29.0 | **AI Build 模式优化（文件读写 + 会话时间戳）**。① Excel 写入新增「追加新工作表」能力：对已存在的工作簿可用 `write --newsheet [名称] 内容` 新增一个 Sheet（省略名称则自动命名 Sheet2 / Sheet3…，指定名称已存在则报错），与原有 Sheet1 等并存，且**完整保留原工作表的字体 / 列宽 / 合并单元格 / 数字格式等样式**（xlsx 走 ExcelJS 读写；xls 走 SheetJS 尽力保留）。此前只能「原地回填 / 追加列」（`--update`）或「整文件覆盖重建」（`--force`，会丢失原表名与样式），无法在已保存工作簿里直接加 Sheet。② 聊天消息时间戳年份由两位（`YY`）补全为四位（`YYYY:MM:DD HH:MM:SS`）。③ AI 回复的时间戳改为按「回复完成」时刻记录，而非请求发起时刻（含停止 / 出错场景）。 |
| 1.0.29 | 1.0.29.0 | **AI 助手新增「快捷提示词」（输入框内集成）**。① 输入框左下角新增 `+` 按钮，点击展开可滚动的已保存提示词列表面板；展开时按钮平滑旋转 45° 变为 `×`，再点收起。② 点击列表中的提示词 → 直接填入输入框（不自动发送，用户可修改后自行发送）。③ 每个提示词右侧支持「编辑 / 删除」快速操作。④ 面板底部「管理提示词」打开管理弹窗，支持增 / 删 / 查 / 改：标题（可选，默认取内容前 30 字）+ 内容，已有列表可逐个编辑或删除；编辑走新增的 `ai:update-prompt` IPC 通道。⑤ 面板底部「保存当前」（输入框有内容时出现）把当前输入一键存为提示词。⑥ 持久化复用 `userData/promptStore.json`（`promptStore.ts`），增删改查均经主进程 IPC，渲染层只通过 `window.mcApi.ai.*` 调用。 |
| 1.0.30 | 1.0.30.0 | **AI Build 模式新增「下载文件到目录」（`file_download`）**。① 新增工具 `file_download`：模型可把 URL 下载到指定目录，参数 `url`（必填）+ `dir`（必填）+ 可选 `name`。② 支持两类来源：**OA 物料规格文件链接**（自动复用当前 OA 登录态，会话失效返回 `NEED_RELOGIN` 并提示先登录）与**任意 http/https 直链**。③ 行为约定：目标目录不存在自动创建（`mkdir -p`）；同名文件自动重命名为「名称(1).ext」「名称(2).ext」，**不覆盖**已有文件；单次下载上限 200MB、超时 60 秒；文件名优先取链接的 `fileName=` 参数，再做非法字符清洗与长度限制。④ 安全：目标目录必须在已授权工作区内，越界返回 `PATH_OUTSIDE_ROOT` 并引导用 `open_folder` 打开；下载全程在主进程完成，**OA Cookie 不出主进程**（也无需注入子进程环境变量）。⑤ 复用：把 `OA_FILE_DOWNLOAD` 里的 OA fetch 抽成 `downloadOaBuffer`，与通用 `downloadBuffer` 一并放入新增的 `src/main/ai/fileDownload.ts`，手动点击下载与 AI 下载共用同一份下载核心（含重定向跟随、超时、体积上限、会话失效识别）；`PARTITION` 常量同步收敛到该模块。⑥ 修复 `promptStore.ts` 的类型错误（`cache` 未收窄为非空导致 `tsc` 报错），类型检查恢复全绿。 |
| 1.0.30 | 1.0.30.0 | **AI 助手会话历史栏：支持收起/展开 + 小窗口抽屉化**。① 会话栏标题左侧新增切换按钮：宽窗口点 `«` 收起为 44px 竖条（只保留展开 `»` 与新建 `+`），再点展开；收起状态写入 `localStorage`（`ai.sidebarCollapsed`）跨启动保留。② 窄窗口（≤760px）改为**左侧抽屉浮层**：侧栏脱离文档流、默认 `translateX` 滑出屏幕外，**不占内容区高度**；工具栏出现 `☰` 唤起，抽屉内头部为 `×` 关闭，外加半透明遮罩点空白处收起，点任意会话后自动收起；滑入/滑出带 0.22s 缓动。③ 新增 `useIsNarrow`（`matchMedia` 侦听 760px 断点），宽/窄切换时自动在「竖条收起」与「抽屉浮层」两种形态间切换，并在窄窗下强制显示侧栏完整内容（用 `.ai-page .ai-sidebar-head` 等选择器覆盖 `side-collapsed` 的隐藏规则，避免抽屉里内容被误隐藏）。④ **删除上一版「小窗压成一条横向栏 + 历史横向滚动」方案**——它会露出原生横向滚动条且白占一整行高度，体验差。⑤ 小窗工具栏紧凑化：内边距与间距减小、服务商下拉限宽 150px、模型输入 180px、输入框最小高度 60px、底部留白 80→72px。⑥ 新增中英双语文案 `aiSideCollapse` / `aiSideExpand`。⑦ 修复实现过程中 `useCallback` 前向引用 `openConversation`（声明在 367 行）导致的 TDZ 隐患，改为在 JSX 就地调用。 |
| 1.0.31 | 1.0.31.0 | **修复「更新进度条满了以后又重新跑一遍」（自动更新重复下载）**。根因：① `UpdateBar` 的 `downloading` 判定含 `pct < 100`——进度到 100% 但 `update-downloaded` 未到时，`downloading` 塌陷为仅看标志位；② 帮助菜单「检查更新」走 `store.checkUpdate()` 时用主进程固定返回的 `downloading: false` 覆盖了进行中的下载状态，导致按钮重现；③ 主进程 `START_DOWNLOAD` 无重入保护，`autoUpdater.downloadUpdate()` 被重复调用会整包重下。修复（四层）：① 新增下载重入保护——`updater.ts` 增加 `downloadInFlight` / `updateDownloadedFlag` 状态位与 `startUpdateDownload()` / `isUpdateDownloaded()`，`update-downloaded` 事件置位、完成或失败自动复位，重复调用直接短路，绝不重复发起下载；② `UpdateBar` 判定改为 `update.downloading || pct > 0`——进度一旦开始，100% 收尾阶段不再把「下载」按钮交回用户；③ `store.checkUpdate()` 不再被固定返回的 `downloading: false` 冲掉进行中状态；④ 帮助菜单「检查更新」先判状态：已下载完成直接提示「新版本已下载」，进行中提示当前进度，均不再触发重复下载。**「关于」弹窗说明更新**：补充内置 AI 助手能力介绍（对话式查询物料 / 库存 / 生命周期，读取与修改本地 Excel / Word / PDF、搜索文件、下载规格文件到指定目录），中英双语同步。 |
| 1.0.32 | 1.0.32.0 | **设置面板 + 系统托盘常驻 + AI 供应商增强 + 英文弹窗修复**。① **英文弹窗修复**：根因是 `dialog:setLang` 异步同步语言竞态；现改为渲染层弹窗时随 payload 携带当前界面语言，主进程 `dialogLang()` 优先采用再回退系统语言，消除中英文错乱。② **新增「设置」面板**（右上「设置」按钮，animal-island 配色）：含语言（中文/English 分段切换）、最小化到托盘后台运行（开关，默认开）、关闭按钮行为（下拉：最小化到托盘 / 直接退出，默认最小化到托盘；总开关关时自动禁用并显「直接退出」）、开机自启（开关，默认关）。③ **系统托盘常驻**：总开关开启时启动即建托盘；左键单击显示主窗口；右键菜单「显示主窗口 / 物料查询（MC 模式）/ AI 助手 / 检查更新 / 退出」，文案随语言切换重建；「物料查询 / AI 助手」显示窗口并切到对应视图（右下角切换按钮高亮同步）。④ **关闭按钮语义**：总开关开 + 关闭行为=最小化到托盘 → ❌ 隐藏到托盘不退出；=直接退出 → 旧行为；总开关关则 ❌ 直接退出。⑤ **开机自启**：`app.setLoginItemSettings`，开启时带 `--hidden` 静默启动到托盘、不弹窗。⑥ **托盘检查更新反馈修复**：原直接调主进程 `autoUpdater.checkForUpdates()` 绕过渲染层反馈、「已是最新 / 失败」时 `UpdateBar` 无提示；现通知渲染层复用 `store.checkUpdate()`，并新增「已是最新版本」提示条。⑦ **托盘图标回退**：`build/` 缺 `icon.ico` 时回退 `icon.png`，避免托盘因找不到图标而不创建。⑧ **自定义 AI 供应商 + 重置为默认**：AI 设置面板「添加自定义供应商」填名称/协议/Base URL/Key/模型，可加多个、下拉按「内置/自定义」分组、自定义项可删；「重置」清空 API Key（内置恢复默认 Base URL+模型），不再需假字符。⑨ 提示词管理/快捷提示词面板等原硬编码中文改跟随语言。 |
| 1.0.33 | 1.0.33.0 | **OpenCode 会话头兼容 + 打包脚本容错**。① **OpenCode（Go / Zen）请求新增 `x-opencode-session` 头**：OpenCode 官方通知自 2026-09-06 起缺失该头的请求将报错——此前本项目的对话请求（`chatService.ts`）与「获取模型 / 测试连接」请求（`providerApi.ts`）均只带 `Content-Type` 与 `Authorization`。现新增 `opencodeSessionHeaders()`：**仅当请求地址命中 `opencode.ai` 时**附加该头——对话请求用**会话 ID**（符合官方 one stable ID per conversation 要求），拉模型与测试连接这类无会话请求用本进程内稳定的应用级 ID；会话 ID 先做 ASCII 清洗（仅保留 `A-Za-z0-9_-`、截断 128 字符），避免含特殊字符导致请求被拒。其他供应商（OpenAI / DeepSeek / Qwen 等）完全不受影响。② **打包脚本容错**：`build_now.ps1` 清理 `dist` 前先 `taskkill` / `Stop-Process` 结束残留的 MC 进程（托盘常驻实例关窗不退出会锁住 `win-unpacked/resources/app.asar`，导致 `Remove-Item` 抛 IOException 中断打包）；删除失败最多重试 5 次（间隔 1 秒，给进程退出与系统释放句柄留时间），仍失败则给出明确提示而非原始异常。 |
| 1.0.34 | 1.0.34.0 | **OA 会话定时检测 + 失效静默自愈 + 查询页失效提示**。① **根因（两个）**：一是 1.0.32 起应用可最小化到托盘长期后台驻留，但登录态检测原本**只在启动时跑一次**（`checkLoginAndNotify`），后台跑一晚、OA 会话过期后 App 完全无感知，只能重启才能重新登录；二是 `requestLoginView()` 只发 `OA_LOGIN_STATE { state:'logging' }`，**没有把渲染层 `loggedIn` 置为 false**，即便主进程检测到失效，登录层也根本不会显示——两个缺陷叠加，表现为「必须关掉 App 重开」。② **新增会话检测与静默自愈**（`checkAndHealSession`，复用已有的 `probeOaSession` / `tryAutoSsoRefresh`）：判定只信真实探测——`reauth` / `invalid` 才判失效，并先走隐藏窗口静默 SSO 自愈（**免扫码**，IAM 仍登录时可静默恢复会话）；`network` 视为无法判定并保持原状态（绝不因网络抖动误踢真登录）；`nocookie` 视为未登录/扫码中，既不自愈也不拉起登录（避免与隐藏登录窗口 `ssoWin` 争抢，1.0.16 教训）。③ **触发时机**：恢复窗口时（托盘单击 / 托盘菜单 / 任务栏 `activate`）检测一次；后台驻留时每 **10 分钟**心跳检测一次——**心跳只负责探测与自愈，绝不主动弹窗**，仅当你把窗口调出、且自愈仍失败时才拉起扫码登录。④ **防骚扰与性能**：in-flight 并发去重 + 60 秒节流，连点托盘不会放大请求；正常探测成功**不写日志**（10 分钟一行会刷屏），仅失效 / 自愈成功失败 / 网络异常时记录 `[SESSION]` 日志，便于回溯「昨晚几点失效」；退出时（`before-quit`）清理心跳定时器。⑤ **修正 `requestLoginView`**：先发 `OA_CHECK_LOGGED { loggedIn:false, reason:'reauth' }` 再发 `OA_LOGIN_STATE { state:'logging' }`，登录层才真正弹出并自动出码。⑥ **查询页失效提示**：历史缺陷是 `store.error` 在整个渲染层**没有任何组件消费**——查询因会话失效失败时，错误只被写进 error 字段，界面上一个字都不显示，用户以为「点了查询没反应」。现新增 `sessionExpired` 状态与 `SessionExpiredBar` 顶部提示条（结构与样式复用 `UpdateBar` / `.update-bar`），显示「OA 登录已失效，请重新登录」+「重新登录」按钮（中英双语 `sessionExpired` / `reLogin`）；该提示条**不依赖扫码层**，即便 `LoginOverlay` 因 IAM 半登录态拉码失败依然可见可用，可随时点「重新登录」重试，登录成功后自动消失。⑦ **保留既有自动重试**：查询遇 `NEED_RELOGIN` 仍先经主进程 `refreshOaSession` 自愈并自动重试一次原查询（自愈成功则用户完全无感），重试仍失败才标记失效并提示。 |
| 1.0.35 | 1.0.35.0 | **仅支持运行一个实例（单实例锁）**。① 新增 `app.requestSingleInstanceLock()`：应用已在运行时再次双击 exe / 桌面快捷方式 / 开机自启，**不再启动第二个进程**——避免多开造成托盘出现重复图标、两份登录态与更新提示互相打架，以及多个进程争抢同一份 `userData` 持久化文件（OA 会话备份、AI 供应商配置、应用偏好都在里面）。② 已有实例收到 `second-instance` 事件时调用 `showMainWindow()`：窗口最小化或正藏在托盘后台时都会被唤回并聚焦（`restore` + `show` + `focus`），用户双击后立刻看到已有界面，而不是「没反应」。③ 唤回时会顺带触发 1.0.34 加入的 `onWindowShownCheck`——后台驻留一晚后双击唤回，即刻检测 OA 会话并自动自愈或拉起登录。④ 若第二个实例以 `--hidden` 启动（开机自启重复触发），保持静默不弹窗，不打扰用户。 |
| 1.0.36 | 1.0.36.0 | **修复 1.0.35 引入的两个体验问题**（1.0.35 已发布，修复随本版发布）。① **修复「已有实例在运行时双击桌面图标，应用会闪一下」**：1.0.35 虽在模块顶层对新进程调用了 `app.quit()`，但 Electron 的 ready 仍可能被触发一次，第二个实例照样执行 `createWindow()` 建出主窗口后随即退出，视觉上就是「窗口一闪而过」。现保存 `gotSingleInstanceLock` 结果并在 `app.whenReady()` 回调开头直接 `return` 兜底——第二个实例**绝不创建窗口**；同时 `showMainWindow()` 改为**仅在窗口不可见时**才 `show()`（已可见时重复 show 同样会闪）。② **修复「点最小化后任务栏图标消失、直接进托盘」**：自 1.0.32 起 `mainWindow.on('minimize')` 里调用 `mainWindow.hide()`，而 `hide()` 会把窗口从任务栏移除，于是点最小化表现为任务栏图标消失、窗口直接跑进托盘。现移除该 hide()，**最小化恢复系统默认行为**（缩到任务栏并在任务栏保留图标）；隐藏到托盘**统一由「关闭按钮行为」控制**。配套把设置项文案 `trayMinimize` 由「最小化到托盘后台运行 / Minimize to tray」改为「后台常驻系统托盘 / Keep in system tray」，使文案与实际行为一致。③ 版本号 1.0.35 → 1.0.36。 |
| 1.0.37 | 1.0.37.0 | **同步更新本地 IMX307 替代料号离线数据**。① 数据源由 `IMX307替代料号明细_260529.xlsx` 更新为 `_260910.xlsx`，映射条数 438 → 501。② 新增第二供应商「罡扇」（2053 方案，30 条），与既有「翔飞」（F355 方案，471 条）合并入映射表，每条新增 `supplier` 字段区分来源；填充此前为空的 `original_desc`（原方案描述）字段。③ 新增可复用转换脚本 `scripts/build_imx307.py`，源文件再更新时一条命令重新生成 JSON。④ 版本号 1.0.36 → 1.0.37。
| 1.0.38 | 1.0.38.0 | **Build 模式桌面路径别名优化 + 越权错误提示修复**。① **桌面预置为可直接写别名 `desktop/`**：用户让 AI「在桌面创建/输出文件」时，AI 原先会错误地把 `C:\Users`（主目录）当成桌面、经 open_folder 得到别名 `users` 再乱拼路径，最终触发 `PATH_OUTSIDE_ROOT`。现用 `app.getPath('desktop')` 动态取真实桌面（自动处理 OneDrive 重定向），预置为 Build 模式文件沙箱默认可用别名 `desktop/`，AI 直接写 `desktop/表格.xlsx` 即落到真实桌面，**不弹「是否允许访问目录」确认框**。② **提示词明确「桌面 = desktop/ 前缀」**：Build 模式系统提示补一句，用户说「桌面/放到桌面」时直接用它，无需 AI 自己猜绝对路径。③ **修复越权错误码与提示不匹配**：原 `file_write` 越权时 `code='PATH_OUTSIDE_ROOT'` 却配中文矛盾文案「路径不存在」；现对齐 `fileDownload` 范本，统一为「目标不在已授权工作区内，桌面请用 desktop/ 前缀」，形成引导闭环，避免 AI 反复猜错路径浪费轮次。 | |
| 1.0.39 | 1.0.39.0 | **主题换肤 + 顶栏下班倒计时 + 应用内 OA 工作台 + 全站 UI 库化（animal-island-ui 1.4 → 1.12）**。① **新增浅色多主题（18 套）**：新增 `src/renderer/src/theme.ts` 主题注册表，每套主题 = 壁纸图案 + 配套配色（覆盖库全部背景类型，按奶油系 / 草木系 / 糖果系三类排序）；设置面板新增「主题」行——当前图案色块 + 名称，点击展开图案网格，缩略图直接用库 `<Background>` 渲染（所见即所得）；选择即时生效并写入 `localStorage(mc-theme)` 跨启动记忆，未选过或值损坏时回落「奶油波点」。换肤链路：主题色写入 `--mc-*`，其余衍生态（卡片底 / 分隔线 / 深色 hover / 浅色 tint）由 CSS `color-mix()` 从 3 个原始色值算出，同时把这些值指向库的 `--animal-*` token，于是**库自带组件（Button / Tag / Select / Switch / Progress / Tabs…）整体跟随主题**，不再需要 `[class*="trigger-"]` 这类命中库内部类名的覆盖写法；首屏在 `createRoot().render()` 之前同步应用主题以消除启动闪色；全局壁纸层上提到 `App` 根部（`.mc-backdrop`），登录页 / 查询页 / AI 页 / 顶部提示条共用同一张壁纸。② **顶栏「距离下班还有 hh:mm:ss」**（库 `<Countdown>`，18:00 下班）：休息日（周末 / 法定节假日 / 调休放假）与已过下班点改为一句祝福文案，避免倒计时僵在 00:00:00。**配套新增法定节假日同步**：新增 `shared/holidays.ts`（内置 2025 / 2026 国办通知表 + 运行时覆盖层 `applyRemotePlan`，没有任何数据的年份退化成「周一至周五上班」）与 `src/main/holidaySync.ts`（主进程逐年拉取，数据源依次尝试 timor.tech → holiday-cn@jsDelivr → holiday-cn@GitHub；用 `net.fetch` 走系统代理；7 天磁盘缓存 `userData/holidays/<年>.json`；全部源失败后 6 小时冷却；返回数据体检不合格（混入别的年份 / 数量越界）一律丢弃，宁可回退内置表也不写坏缓存），IPC 新增 `HOLIDAY_GET`；渲染层只在「下班时刻 / 次日零点 / 判定结论变化」时重算，不挂每秒计时器，另有 30s 时钟守卫应对系统时钟跳变与休眠唤醒。③ **新增应用内「OA 工作台」窗口**：点标题栏品牌图标即在新窗口打开 OA 首页，窗口共用 `persist:mc-query` 分区，**因此免登录**（`mc-open-oa-window`）；按住 Ctrl / ⌘ 点击仍走系统默认浏览器；已开着窗口时再点只聚焦、不叠窗，标题固定「OA工作台」。**修复其中一个关键坑**：OA 走明文 http，而会话 cookie（route / SESSION / LtpaToken）全带 `Secure`，Chromium 不会在明文连接上发送、也拒收 Set-Cookie，导致窗口内永远处于无会话状态、oa ↔ iam 互相 302 形成死循环、页面全白（主进程自身接口是手工拼 Cookie 头发送的，所以表现为「查询正常、应用内打开空白」）；现仅对 OA 窗口自身的请求补 Cookie 头绕开该限制。品牌图标提示改用库 `<Tooltip variant="default">`。④ **全站 UI 库化治理**：整体删除 `.btn` / `.mq-btn` / `.animal-btn` 三套自建按钮样式与 `--mc-btn-*` 度量 token、`.mq-mini-btn`、`.mq-disclosure`、`.ai-tool-run` 等自绘控件样式；按钮统一库 `<Button>`（含 danger / ghost / 图标按钮）、图标统一库 `<Icon>`（库无 chevron，统一用 `Play` 三角旋转表达方向）、标签用 `<Tag>`、提示气泡用 `<Tooltip>`、时钟 `<Time>`；批量查询折叠壳与 AI 工具调用卡改用库 `<Collapse>`（自绘的 +/− 徽标、高度过渡、箭头旋转全部交回库）；AI 工具详情由裸 `<pre>` 改为库 `<CodeBlock>`（保留超长返回的纯文本兜底）；AI 供应商 / 协议下拉由原生 `<select>` 改为库 `<Select>`（自定义供应商入口并入下拉末项）。⑤ **全局光标**：改用库 `<Cursor type="default">`（手指箭头），并在 `cursor.css` 补回输入框 I 型光标与禁用态 `not-allowed`。⑥ **中英文案补全**：登录页原先整页写死中文（切英文后不生效），现标题 / 副标题 / 扫码状态 / 各类错误与刷新文案全部走 i18n；CSV 导出失败提示、AI 快捷提示词、设置面板等硬编码中文一并改走 i18n；表格里的**取值**也支持英译（新增 `translateItemType` / `translateLifecycle`，物料类型与生命周期枚举切英文时同步显示英文，但筛选 / 排序 / 导出仍用原始值，避免换语言改变筛选语义）。⑦ **AI 助手工具栏精简**：移除「MC Skill」开关与工作区选择按钮（模式「对话 / 物料 / Build」本身已区分下发哪些工具；Build 模式不再强制先选工作区，首次需要目录时由模型调用 `open_folder` 引导授权，桌面仍预置为 `desktop/` 别名）；Markdown 编辑器入口改用库 `<Tooltip>`。⑧ **其他**：BOM 空态补 PLM 系统入口链接；搜索条件行加「拖动调整顺序」提示；物料 / BOM 行展开区按钮统一 `<Button>` + `<Icon>`；修复 `src/main/ai/rootGuard.ts` 把 `existsSync` 从 `path` 误导入的问题。⑨ 版本号 1.0.38 → 1.0.39，`buildVersion` 同步 1.0.39.0。 |
| 1.0.40 | 1.0.40.0 | **OA 工作台免登录贯通到子窗口 + IAM 会话独立保活 + 内网链接统一走应用内窗口 + 全站下拉统一自绘 + 顶栏品牌区整块可点**。① **修复 OA 工作台里点入口弹出全白窗口**：OA 门户的「新建流程」「会议预约」「我的工具」等入口大多是 `target="_blank"` / `window.open`，原实现只给主 OA 窗口补 Cookie，子窗口不经过注入 → 又回到「明文连接不发送 Secure cookie」的死循环，表现为弹出一个全白窗口、标题还退回应用名。现给 OA 窗口安装 `setWindowOpenHandler`，子窗口正常创建并**递归登记**为「OA 免登录窗口」（子窗口里再弹一层也不会漏），继承 `persist:mc-query` 分区、隐藏菜单栏与父窗口归属；`mailto:` / `tencent://` 等非 http(s) 链接交系统处理，不再开空窗口；`did-create-window` 里恢复最小化窗口并聚焦。② **Cookie 注入按「窗口集合 + 泛域名」收敛**：匹配范围从 `*://oa.streamax.com/*` 放宽到 `*://*.streamax.com/*` 与 `*://streamax.com/*`（窗口内的入口会跳到 IAM / MC 等兄弟站点，只注入 OA 会让跳转后的页面继续停在登录页），并改为只对登记过的 webContents 注入，应用主窗口（查询页 / AI 页）与其它后台请求一律放行、不受影响；子窗口不再钉标题（任务栏里能分辨是哪个页面），主 OA 窗口仍固定「OA工作台」。③ **全站下拉统一自绘 `.mc-select`**：库 `<Select>` 的面板是 #ffeea0 底 / 28px 圆角 / 条目居中 / 黄色药丸高亮 / 橙色三角光标，且内联写死 `left: 100%`、从触发器**右侧**弹出 —— 用在整行变体上时：新增自定义供应商弹窗（`.ai-prompt-modal` 为 `overflow: hidden`）里「API 格式」面板整块被裁到容器外、点了像没反应，设置面板的协议下拉则挪到窗口左侧压住会话栏。现五处（物料类型、AI 供应商、API 格式 ×2、关闭行为）全部换成自绘 `McSelect`：面板规格与 `.ai-model-panel` 逐条一致（surface 底、1px 描边、14px 圆角、6px 内边距、条目 6px 8px + 主题色 14% 悬停、选中为主色渐变），触发器沿用 `.lifecycle-trigger` 的 40px / 12px / 2px 规格（AI 工具栏与设置面板用 34px 紧凑变体），并排控件天然齐平。④ **面板改 `position: fixed` + 运行时定位**：绝对定位仍会被弹窗 / 设置面板祖先的 `overflow` 裁掉，故改为以视口为包含块、按触发器实测坐标定位 —— 滚动与窗口缩放跟随、下方放不下且上方够时自动向上翻、整行变体与触发器等宽、右边缘越界时整体左移；首帧先隐藏占位，不会闪到错位置；交互与库行为对齐（点击外部关闭、Esc、Enter / Space / 上下键、`role=combobox/listbox/option`），随之删除失效的 `.settings-select-wrap` 与 `[class*="trigger-"]` 覆盖。⑤ **顶栏品牌区整块可点**：原先只有 56px 的图标可点，点标题 / 倒计时「没反应」；现 `.brand-icon-btn` → `.brand-btn`，图标 + 标题 + 倒计时同在一个按钮内（hover 缩放也覆盖整块），点标题同样打开 OA 工作台。⑥ **修复「应用内 OA 工作台本身免登录正常，但在窗口里点入口跳到 IAM 时仍要求重新扫码」**：根因是 **IAM 会话有独立于 OA 的 idle 超时**——OA 会话靠 `LtpaToken(@.streamax.com)` 被会话心跳长期续命、几乎不会失效，应用因此从不主动碰 IAM；而 IAM 侧会话（`SESSION` / `usk` / `REQID` @ `iam.streamax.com`）在后台一路闲置到过期（实测登录约 3 小时后 `GET /idp/authCenter/authenticate` 不再 302 带 `code` 回 OA，且响应顺手把 `REQID` 置为 `expires=1970`），IAM 一旦失效**无法静默恢复、只能重新扫码**。新增 `keepIamSessionAlive`：定期 `GET` IAM 的 OAuth 授权端点但**不跟随重定向**（读到响应头即断开，不产生 OA 落地副作用），只为刷新 IAM 侧 session 的 idle 计时，并把响应回写的票据（如新 `REQID`）同步写回分区；判据为 `Location` 回到 `oa.streamax.com` 且带 `code`。触发时机两处：会话心跳（每 10 分钟，登录态有效时）+ 打开应用内窗口前（后台并行，不阻塞窗口出现）。in-flight 去重 + 8 秒超时，纯后台、失败绝不影响 UI；日志 `[iamKeepalive] status=... iamSessionAlive=...` 便于回溯。⑦ **内网链接统一走应用内窗口，不再送系统浏览器**：系统浏览器的 cookie 库与本应用的 `persist:mc-query` 分区**完全隔离**，且 OA / IAM 都没有「用 URL 传票据」的入口（IAM SPA 的 `lck` 是一次性 SSO 上下文，不是凭证），把内网地址交给系统浏览器**必然**被 302 到 IAM 登录页要求重新扫码——实测落点即 `https://iam.streamax.com/ac/#/index?lck=...&entityId=oa&theme=...`。这是操作系统级的机制限制，不是可绕过的实现问题。现统一收口：① 顶栏品牌区（图标 / 标题 / 倒计时）**去掉「Ctrl / ⌘ + 点击 → 系统浏览器」分支**，一律应用内打开；② AI 回复里的 `*.streamax.com` 链接改为应用内窗口打开（外部站点仍走系统浏览器）；③ 主窗口内导航到内网地址原先是「静默吞掉」（点了像没反应），现改为应用内窗口打开；④ `mc-open-oa-window` 支持可选 URL，任意内网地址都能在应用内窗口打开——同一地址只聚焦不叠窗，并继承 Cookie 补齐与子窗口接管；⑤ Cookie 补齐注入器提取为 `ensureOaCookieInjector`，两条开窗入口都会装载（含按 host 只打一次的 `[oaCookie] inject` 诊断日志）。⑧ 版本号 1.0.39 → 1.0.40，`buildVersion` 同步 1.0.40.0。 |
| 1.0.41 | 1.0.41.0 | **设置面板内容可滚动 + 输入框悬停手感统一**。① **修复「主题网格展开后，设置面板下方的设置项够不着」**：设置面板挂在顶栏下方，内容是高柱（主题 18 套图案网格展开后整体可超 600px），窗口高度不足时「关闭按钮行为 / 开机自启」被挤出窗口底部，而面板本身没有任何滚动入口，这些设置项直接不可达。现给 `.settings-panel` 加高度上限 `calc(100vh - 160px)` 与 `overflow-y: auto`：超出部分改为面板内滚动查看；同时加 `overscroll-behavior: contain`（滚到底不带动外层页面）与 `scrollbar-gutter: stable`（预留滚动条宽度，滚动条出现/消失时主题网格不会左右跳）。窗口足够高时面板观感与原先完全一致。② **统一输入框的悬停手感（关键词筛选 ↔ 搜索条件）**：两处输入框引用的是同一个 `.mq-input`，但旧代码把「悬停时保住底部 3px 立体投影」的规则**限定在 `.hist-input` 作用域内**（只有搜索条件的 `HistoryInput` 会多包一层 `.hist-input`），于是同一个类名在两地表现不一致——**关键词筛选**：鼠标放上去底部投影消失、控件像被「按下去」；**搜索条件**：悬停时投影纹丝不动，毫无反馈。现按关键词筛选**原有的手感**统一：悬停 → 底部投影消失；并把该效果从「库 `no-shadow` 悬停规则的副作用」显式写成我们自己的规则（`.mq-input` 写两遍凑到 (0,3,0) 以压过库那条同名规则），库换哈希类名 / 改默认形态时行为不漂移。聚焦态同样提到 (0,3,0) 并置于悬停之后，避免「悬停 + 聚焦」并存时聚焦换不上主题色。两处输入框的描边、立体投影与悬停 / 聚焦动效自此完全一致。③ 版本号 1.0.40 → 1.0.41，`buildVersion` 同步 1.0.41.0。 |
| 1.0.42 | 1.0.42.0 | **全面性能优化（切视图 / 输入 / 流式 / 展开）+ 生命周期英文补录**。① **切视图不再重渲染整个界面**：`App` 里给 `QueryPanel` / `ChatPanel` / 壁纸 `Background` 加 `memo` —— 此前 `setView` 以及语言、登录态、SSO 遮罩等任一订阅变化都会把「查询页表格（结果集行数量级 DOM）+ AI 页消息列表（消息数 × Markdown 解析）」两棵大树一起重渲染，这是切视图卡顿的主因；`MaterialTable` / `BomTable` 同样加 `memo`（此前在料号、批量、搜索条件输入框里每敲一个字，父组件重渲染都会把整张表连带重渲染）。② **AI 流式输出三处优化**：delta 改为「按帧合并」（`requestAnimationFrame` 累积后统一 flush，渲染次数从每 token 一次降到每帧一次；`done` / `error` / `tool-*` / 切会话前强制 flush，不漏字不串会话）；`MessageItem` 加 `memo`；Markdown 渲染结果按 `content` 记忆化（此前每来一个 token 都会把**全部历史消息**重新解析 + 重新高亮一遍）。③ **工具调用卡：真正写进 DOM 的 JSON 做截断（3000 字符 / 50 行）** —— 根因是库 `<Collapse>` 的 answer **一直挂在 DOM 里**（靠 `grid-template-rows` 收起，折叠并不等于没有成本），而一次 `mc_query` 的 output 可能有几百行 JSON（实测 148 / 197 条记录），十来张卡叠起来就是 MB 级文本常驻对话区，同时造成「在 AI 输入框打字发卡」与「展开工具卡卡顿」；现按上限截断，并在截断时给「复制完整内容」按钮（信息不丢），截断内容走纯文本 `<pre>`（避免 `CodeBlock` 把几千字符切成上千 `<span>`）；`ToolRunCard` 同时加 `memo`。④ **布局隔离 `contain: layout style`（`.ai-messages` / `.ai-composer` / `.table-area`）**：Blink 在没有布局边界时，任何深层节点的改动都会把整篇文档标脏，于是打字、流式、展开都会带上另一个视图面板一起重排；这三处内部都不含 `position: fixed` 弹层（`<BackTop>` 是 fixed、`McSelect` 面板也是 fixed，故 `.panel-body` / `.view-pane` / `.ai-main` / `.filter-bar` 一律不加，否则弹层基准会被改）。⑤ **去掉昂贵常驻合成层**：`.view-switch` 去掉 `backdrop-filter: blur(4px)`（浮层常驻可见，背后内容一变就要重新模糊采样）改 96% 实底；`.panel-locked` 由 `filter: blur(2px)` 改 `opacity: .55`；登录遮罩整窗模糊 6px → 3px；新增 `html.win-idle`（窗口失焦 / 最小化 / 切后台时暂停选中 Tab 小叶子、壁纸入场等装饰性动画）。实测 GPU 进程 124.8 MB → 100.7 MB。⑥ **启动路径收敛**：`preload` 所有 `on*` 改为返回「取消订阅」函数、`App` 统一清理 —— 修掉 StrictMode 下 effect 双跑导致的两份监听（登录态、更新进度被处理两次）与 `reloadLogin` 重复触发；`reloadLogin` 推迟到首帧渲染之后；节假日同步改用 `requestIdleCallback`（不支持则 250ms 后退化）。⑦ **关键词 / 类型筛选防抖 120ms**：输入框的值仍即时更新（手感不变），只把「重算 filtered → 整表重渲染」推迟到停止输入之后；导出 CSV 前先 flush，避免导出到旧结果集。⑧ **生命周期英文翻译补全**：`试产样品`（OA 真实枚举，我们自己的 `mc_query.js` 里一直有它，但最初建字典时漏收）补为 `Trial Sample`，并同步补 `STATUS_TAG_COLOR`（app-blue）与 `STATUS_CLS`（mq-blue）；另给取值字典加了「去掉 `[xxx]` 分类前缀再查」的归一化（本地 IMX307 明细里就是 `[产品]量产` 这种写法），避免接口换前缀写法又漏翻译。⑨ **新增开发模式自测探针**（打包后摇掉）：切视图打印 `react / layout / paint` 三段耗时，AI 输入框只在「输入到下一帧 >30ms」时打印，便于后续继续定位性能问题。⑩ 版本号 1.0.41 → 1.0.42，`buildVersion` 同步 1.0.42.0。 |
| 1.0.43 | 1.0.43.0 | **AI 助手交互改版（参考 CodeBuddy）+ 技能体系与内置「鸿翼文件查询下载」+ 附件 + 待发队列 + 增强提示词**。① **模式切换改为下拉菜单**：原三段胶囊 → 触发器（当前模式图标 + 文案 + ▾）+ 菜单项（图标 + 名称 + 一句说明 + 选中勾选），**文案仍是 对话 / 物料 / Build，`setMode` 语义、工具下发规则与三模式行为完全不变**。② **新增技能体系**（`src/main/ai/skillRegistry.ts`）：内置（`resources/skills/*`，目录里有 SKILL.md 即算一个技能，新增内置技能不必改代码）与导入（`userData/skills/*`，**支持 .zip 与文件夹两种入口**，同 id 覆盖＝更新）统一管理；启用状态**按会话维护、不再落盘**（`userData/ai-skills.json` 已废弃，`SKILL_TOGGLE` 通道一并去掉）：勾选**只对当前会话生效** —— 新建会话从零开始、切回旧会话仍是原样、进程重启后全部不勾选（此前是全局持久化，于是出现「上一个会话勾了，新建会话也照样勾着」）；技能**默认全部不勾选**（内置技能也一样），用「导入」进来的会自动勾到当前会话（导入是明确动作），避免「内置技能默认勾选＝每次 build 对话都白烧一份 SKILL.md 提示词」；会话 id 是「新会话」时用 `__new__` 槽，首条消息落库拿到真 id 后把勾选迁移过去再清空该槽（否则发送后技能会被悄悄丢掉）；未启用的技能只把 `id + 名称` 列一行给模型（不注入正文，成本可忽略），模型据此提示用户去 Skills 面板勾选，而不是回答「我做不到」；面板里一条都没勾时另给一句明示「技能默认不启用；勾选只对当前会话生效，新建会话需重新勾选」；**内置与导入同名时两条都列、勾选状态各自独立**：唯一键改成「**来源:id**」（`shared/ai-types` 的 `skillKey()`），勾选、注入提示词、下发工具全用它 —— 此前只按裸 id 记录，面板里两个同名项会共用一条状态（表现是「勾一个、另一个跟着变」）；中途一度改成「导入遮蔽内置、只列一条」，但用户需要看到两份，故最终改为共存：`skillPrompt` 支持「来源:」前缀解析（注入的就是勾中的那一份），同名的两份同时启用时下发的**工具名会去重**（重复 function name 会被接口拒），删除导入的那份会同步清掉它的勾选记录；IPC 走 `AI_IPC` + preload `mcApi.ai.*`（list / import / toggle / remove）；`build` 模式下把已启用技能的 SKILL.md（逐个截断 12KB）注入系统提示，并按 `SKILL_TOOLS` 映射下发其工具。两个「实现层技能」`mc-material-query-local` / `file-office-local` 由 `INTERNAL_SKILLS` 白名单排除 —— 它们的行为已硬编码在工具定义与 mcSkill/fileSkill 提示里，列进面板只会把 20KB+ 提示词白塞进每次 build 会话。③ **内置「鸿翼文件查询下载」（wjxt-file-download）**：原包靠 `agent-browser`（外部 Chrome + CDP）扫码登录后在页面上下文 eval fetch，而本应用启动即完成钉钉登录、也没有该 CLI，故**去掉登录部分、改为主进程直连**（`src/main/ai/wjxtSkill.ts`）：用 `session.fromPartition(PARTITION).fetch` 发请求（自动带分区 cookie、走系统证书库与代理、自动跟随重定向）——`POST /WebCore` 搜索（module=WebClient、fun=GetMapSearchResultList）→ `GET /Preview/GetPreviewPara` 取 `data.fileUrl` → 按契约把 `GetConversionFile` 改造成 `GetOriginFile`（x 只取前 3 段、去掉 r 参数）拿**原始文件**（不转码）；下载复用 `downloadToDir` 的「目录必须在已授权工作区内 / 同名自动重命名 / 200MB 上限」约束（为此给 `downloadBuffer` 与 `downloadToDir` 增加 `referer` / `useSession` / `cookie` 参数），**Cookie 全程不出主进程**；工具 `wjxt_search`（返回编号清单，供用户按编号确认）与 `wjxt_download`（单/多个 GUID 批量下载，回报每个文件字节数），**仅 build 模式 + 已启用该技能时下发**；错误码统一转成可执行提示（`NEED_RELOGIN` → 提示在应用内重新登录）。技能包 `resources/skills/wjxt-file-download/` 只含重写后的 SKILL.md（触发词、确认流程、下载规则、错误处置、禁用 agent-browser）与 references，执行层在主进程，**不随包分发脚本或运行时**；`electron-builder.yml` extraResources 增加该目录。④ **附件（图片内联 + 文本内联 + 其他按路径）**：粘贴截图 / 拖拽文件 / 复制文件后粘贴 / 选择文件都变成输入区上方的 chip（可单个删除、可一键清空）——图片按最长边缩到 1568px 并压成 JPEG（透明 PNG 保持 PNG）后内联，文本类（按 mime 或扩展名识别）读成文本内联（>200KB 截断并在 chip 与消息里标注），xlsx/docx/pdf 等只记本机路径并在 build 模式交给 `file_read`（非 build 模式给出「切到 Build」的提示）；主进程 `buildUserContent()` 按 OpenAI 兼容协议拼 content 数组（`image_url` dataURL），**无附件时仍返回纯字符串**（与旧行为逐字兼容）；附件随消息落库（`AIMessage.attachments` + `historyStore.appendMessage`），历史回放时图片与文本继续生效（追问不必重发），消息气泡上方回显 chip。**图片附件 chip 悬停（或键盘聚焦）会在 chip 上方浮出原图 + 文件名/大小** —— 此前只有一个 20px 缩略图，两张截图放在一起根本分不清哪张是哪张；已发送消息里的附件回显同样支持（它的预览朝**下方**展开：回显在对话流里，而列表本身 `overflow: auto` 会裁剪，第一条消息贴顶时向上弹会被裁掉）。预览层 `pointer-events: none`（不被它自己截走 hover 造成闪烁），两处 chip 顺手抽成同一个 `AttachChip` 组件，不再两份 JSX 各自维护。⑤ **待发队列升级**：原先只是一排药丸，现改为输入区上方的可操作列表 —— 拖动排序 / 立刻发送（生成中则插到队首并提示）/ 取回输入框继续编辑 / 删除，附件数量随项显示；排队项把附件一起快照，避免输入区清空后丢附件。⑥ **生成中可另起会话**：点侧栏「新建对话」即可立刻开一条并发会话并发送（底层本就支持多会话并发），侧栏对正在生成的会话显示脉冲标记。⑦ **增强提示词**：发送按钮左侧新增按钮，点一次用**当前供应商**把草稿改写成更明确、AI 更懂的要求（新增 `ai:optimize-prompt`，一次性非流式请求、不落历史、不产生会话），结果**直接替换输入框**且不自动发送，并给一次「撤销」。⑨ **修复内置技能「鸿翼文件查询下载」首次使用必失败（`WJXT_BAD_RESPONSE`）**：根因是 edoc2 对**未登录**请求不是回 401，而是 `302` 到 SSO 登录页（实测 `Location: /https://wj.streamax.com:9443/sso/auth/goToLoginPage?returnUrl=%2FWebCore`），而实现用的是默认「跟随重定向」的 `session.fetch` —— 最终拿到的是一整页登录 HTML，`JSON.parse` 失败后被笼统报成 `WJXT_BAD_RESPONSE`，把排查方向带偏成「服务端抖动」。现改为：① 判据改为「**跟随重定向后的最终 URL**（`res.url`）命中 `goToLoginPage` / `/sso/`，或 content-type/正文是 HTML」即判为登录态问题（**不能**用 `redirect:'manual'`：Electron 的 `session.fetch` 会把 3xx 直接取消成 `Redirect was cancelled` 请求错误，根本拿不到响应头 —— 这一版先踩了这个坑，已回退为「跟随重定向 + 看最终落点」）；② 响应统一走 `readJson` 判读，按 `401/403`、`nResult=601`、**HTML 页面（含 content-type 判据）**、`404`、`5xx`、非 JSON 依次给出 `NEED_RELOGIN` / `WJXT_NO_SESSION` / `WJXT_HTTP_404` / `WJXT_HTTP_5XX` / `WJXT_BAD_RESPONSE`（后者带上 status / content-type / body 片段便于定位）；③ **显式补 Cookie 头**（`cookies.get({ url })` 后手工拼 `Cookie`，与 OA 侧窗口 `ensureOaCookieInjector` 同一套做法）——`session.fetch` 属非浏览器上下文，对 `SameSite=Strict/Lax` 这类 cookie 不保证自动附加，会出现「分区里明明有该域 cookie、服务端却仍 302 到登录页」；④ **会话缺失/失效时自动弹出应用内登录窗口**（新增 `setWjxtLoginOpener`，由 `index.ts` 注入 `openInternalUrl`，与工具请求共用 `persist:mc-query` 分区，登录一次即可复用），并告诉用户/模型在该窗口内登录后重试，**不再引导去系统浏览器**（不共享登录态）；⑤ 下载侧同源收口：`downloadBufferViaSession` 对「最终落到 `iam.streamax.com` / `goToLoginPage` / `/sso/`」与「期望文件却收到 `text/html`」一律判 `NEED_RELOGIN`，避免把登录页 HTML 当文件写盘；⑥ **按站点前端的启动序列做「会话预热」**：`GET /index.html` → `POST WebCore {SystemManager/GetSystemInitStatus}` → `POST WebCore {WebClient/GetCurrentUser}`（带 `token` cookie，值只在内存流转、不出主进程）—— 实测**带着分区 cookie 直连业务接口会拿到 404 + 空 body**，与「未登录 302 到 SSO 登录页」是两种完全不同的形态（据此可区分「会话缺失」与「会话上下文未建立」），而站点前端源码（`/scripts/app/main.js`）的启动序列正是这两步、且它专门把 404 静默掉，原技能包对这一形态的处置就是「重新导航一次首页」。现首次搜索前预热一次（进程内只做一次），仍 404 时**强制重做预热并只重试一次**，其余失败如实上报；请求头补齐站点 jQuery 同款 `X-Requested-With: XMLHttpRequest` 与 `Accept: application/json, ...`，日志追加 `names=<cookie 名字列表>`（只记名字、不记值）；⑦ 新增 `userData/wjxt.log` 诊断日志（每次请求记 `status / final / content-type / 长度 / 拿到的 cookie 数量与名字`，**不含 cookie 值**），SKILL.md 与 references/notes.md 的错误码表同步补齐。⑧ **按上游技能包「实际跑通」的写法修正查询构造（0 条的真正原因）**：上游包 `search_edoc2.ps1` 里的查询是 `(filename:(kw) OR filecontent:(kw))`，**不带** `(filepath:(1) OR masterfilepath:(1))` 范围前缀，而它自己的 `api_reference.md` / `prepare_urls.py` 却带前缀 —— 用 `agent-browser` 在真机上对比实测：**带前缀 0 条、去前缀命中 50 条**（HTTP 200、无任何报错）。现首选查询改为无前缀写法，0 条时才退回带前缀那条重搜一次；关键词转义同时放宽（保留 `*` 通配与 `. _ -` 等文件名字符，只对 `( ) [ ] { } " ^ ~ :` 与 `&&` / `||` 做语法转义），修掉「`ADPLUS*` 被转义成空格导致查询失效」。另一半真因是**「未登录信封」被误判成「0 条」**：服务端未登录时返回的是 HTTP 200 的**合法 JSON** `{"url":"…/sso/auth/goToLoginPage","returnUrl":"/WebCore","defaultUrl":"/index.html","errorCode":"ErrorCode4"}`（站点 `main.js` 的 `$.ajaxSetup.dataFilter` 正是按 `errorCode === 'ErrorCode4'` 跳登录页），而旧实现只按「是不是 JSON / 是不是 HTML」判读，于是把它当成「搜索结果为空」——日志里表现为「请求成功、`errorCode: ErrorCode4`、0 条」。现按站点自己的判据识别 `errorCode` / `url`，报 `WJXT_NO_SESSION`；**登录自愈顺序**（尽量不打扰用户）：① 静默 SSO（主进程 GET 站点登录入口并跟随重定向）→ ② **隐藏窗口预热**（`show:false` + `backgroundThrottling:false` 的窗口加载 `/index.html`，让站点 SPA 自己完成握手）→ 每步之后都用 `GetCurrentUser` 复查。**实测结论**：该登录页不会自动完成登录 —— 分区里即便有给 OA 用的 `LtpaToken` 也照样返回 `ErrorCode4`，隐藏窗口跑完只多出 `copy`/`cut` 两个 SPA 自己的 cookie；**首次使用或会话彻底过期时，必须由人工在登录页扫码一次**（工具无法代扫）。所以静默两步只是「会话仍有效时悄悄恢复」的手段；真需要人工时，可见登录窗口**直接落在 SSO 登录页**（不再先开一屏首页），并且**登录成功后自动关闭**（主进程每 2s 用 `GetCurrentUser` 复查、最多等 3 分钟，成功后还会复位预热状态让下一次搜索直接干活），用户不必自己找关闭按钮。日志里的 `names=` 也确认了：分区当时只有 `checkToken` / `LtpaToken` 两张给 OA/SSO 用的票，**没有 edoc2 自己的会话** —— 「应用已完成钉钉鉴权」并不等于 edoc2 已登录。同时**复用既有「锐明文档站增强搜索」脚本（1.5.0）的三处已验证技巧**：① `mode`（`word` 分词 / `contains` 子串 `*词*` / `exact` 整串）—— `contains` 会自动 OR 出大小写变体，因为**通配符大小写敏感**（该脚本实测 `*RED*` 匹配不到 `Red.png`）；② `sort`（`score` / `time` / `size` / `name`），`time` 让服务端按 `modifyTime` 倒序返回，直接回答「哪个版本最新」，不必自己排序；③ `from` 分页（返回里带 `hasMore`），并在 `query` 的说明里写明可用字段（`filename` / `filecontent` / `extName` / `creatorName` / `modifyTime` / `filepath`）与区间写法 `modifyTime:[2026-01-01 TO 2026-12-31]`。⑨ **搜索结果字段对齐上游包**：补 `modifyTime`（服务端 `/Date(毫秒+0800)/` → `YYYY-MM-DD HH:mm`）、`createTime`、`path`、`lastVerNumStr`、`creatorName` / `editorName`、`id` / `parentFolderId` —— 「哪份/哪个版本最新」这类问题只看文件名答不了，必须靠时间与版本号。⑩ **下载侧补「文件头校验 + 精确字节数比对」**（上游 `download.ps1` 的做法）：按 magic bytes 识别 PK/PDF/OLE2，**拿到 HTML（登录页）直接判登录态失效**，不再把登录页当文件写盘；`wjxt_download` 新增 `sizes` 参数（与 `fileGuids` 一一对应），返回里给出 `kind` 与 `sizeMatched`；下载直链可能落到**区域主机**（实测 `wjhw.streamax.com:9443`，另见 `wjcq`），走会话下载 + URL 自带签名 token，无需跨域 cookie。⑪ **空结果留痕 + 响应结构兜底**：结果数组路径做多重兜底（`docListInfo.FilesInfo` / `data.docListInfo.FilesInfo` / `filesInfo` …）—— 取错路径会表现为「解析成功但 0 条」，看起来像「库里没这个文件」，实际是解析拿空了；`count=0` 时把关键词、实际用的 ES 查询串、`result` / `errorCode` / `islogin`、两级 key 名与响应前 600 字符写进日志，据此区分「查询条件/权限问题」与「服务端结构变了」。⑫ **搜索效率优化（实测驱动）**：一段真实使用日志里，模型为一个文件发了 **88 次搜索、其中 74 次返回 0 条**（40 多个关键词来回试），回复周期极长。现把「换个写法再试」收进**工具内部**：`wjxt_search` 一次调用按**策略链**依次尝试「原模式 → `contains` 子串 → 拆词 OR → 带范围前缀」，命中即停，并在返回里给出 `strategy` 与 `note`（「原查询没命中，已自动改用 X 命中 N 条」），SKILL.md 与工具描述都要求模型**看到 note 就直接用、最多再调一次**、拿到清单就一次性给全结论（含副本、时间、版本先后），不再「我再补两轮查询」；拆词兜底按长度取前 3 个 token（实测完整长名 `HY_ADPLUS2.0_M0010_V3_2.4.6_RC26042090` 直查常 0 条，拆出 `RC26042090 / ADPLUS2 / M0010` 命中率高得多）；同时明确「含数字/标点的长片段别用 `contains`——通配符在该字段上对这类片段不敏感，用 `word` 或短片段」。此外新增一节**硬约束「只在服务器检索」**（优先级最高）：禁止用本地文件工具（`file_search` / `list_dir` / `file_read`）去「补充检索」同一个文件、禁止提议「把本地目录路径发我」、禁止拿 `*` 这类当「探测查询」去试库是否为空；结论只能来自 `wjxt_search` 的返回值（`count:0` 就如实说按这些关键词没命中，并给出更宽的关键词建议，**不断言**「文件不存在」、**更不**转去本地找），只有用户**主动**给出本地路径时才用本地工具（那是另一件事）。`wjxt_search` 的工具描述也补了同一句，避免模型又把一轮往返浪费在本地扫描上。⑬ **服务端排序会静默返回空集（另一处「0 条」真因）**：同一关键词、同一登录态下，`searchXml.sort` 用 `[{_score:…}]` 命中 26 / 50 条，换成 `[{modifyTime:{order:'desc'}}]` 却是 **0 条**（HTTP 200、`result:0`、`FilesInfo` 为空、无任何报错）—— 而 SKILL.md 刚让模型「要最新就用 `sort:'time'`」，于是整轮查询全 0，还被误读成「文件不存在 / 无权限」。上游「增强搜索」脚本本来就带一个 `sortTime` 能力探测（不支持则退回相关度排序），照搬 `sortClause` 时漏了这层判定。现改为：非 score 排序先按服务端排序试一次，**0 条立刻用相关度排序重跑同一查询**，命中后把该键记为「服务端不支持」、结果在**本地**按该键排序，并在返回的 `note` / `sortMode`（如 `local:time`）里说明；同一进程内后续直接用「相关度 + 本地排序」。⑬ **搜索结果给出可点击的目录/文件入口**：每个结果新增 `folderPath`（服务端 `relativePath` 的**真实路径名**，如 `企业文档库/AD PLUS 2.0/版本发布`）、`folderUrl`（`index.html#doc/enterprise/<folderId>` —— 站点 SPA 的路由形式，与上游「增强搜索」脚本的 `navigateToFolder` 写法一致，已互证）与 `previewUrl`（`/preview.html?fileid=<fileGuid>`，同脚本也用这个入口）。SKILL.md 要求：「目录」列的**链接文字用 `folderPath`**、href 用 `folderUrl`，文件名用 `previewUrl` 做链接 —— 此前表格里只有一串 `1/913/921/…` 数字、或写成无信息量的「浏览该目录」，用户既看不懂也点不动；应用已把 AI 回复里的 `*.streamax.com` 链接改为**应用内窗口打开**，现在点目录能直接浏览、点文件名能直接预览/下载。⑭ **AI 输出时的「回到最新输出」按钮**：滚离底部（>80px）时在输入区上沿外侧浮出一个圆形按钮，点击平滑回到底部；生成中额外带一个呼吸小点，提示「上面还有新内容」（`html.win-idle` 时暂停该动画）。同时把「主动发送」与「被动流式增量」的滚动策略分开：主动发送后无条件跟随到底部，流式增量仍只在你本来就贴底时才跟随 —— 上翻历史不再被强行拽回，改为按钮兜住。⑮ **搜索请求改由隐藏窗口的页面上下文发起**（「无报错但 0 条」的最终真因）：实测**同一个登录态、同一个 `/WebCore` 接口、同一份表单参数**，两条通道的结果完全不同 —— 主进程 `session.fetch`（显式补 Cookie 头 + `Referer: preview.html`）返回 HTTP 200 但 `FilesInfo` **恒为空**（表现为「搜不到任何文件」），而上游技能包用 `agent-browser` 在**页面上下文** `fetch('WebCore')` 时同一关键词**命中 50 条**真实数据、并能下载到原始文件（本机已完整跑通：`total=50`，下载到一份真 DOCX：PK 头 / 15632 字节 / 字节数校验通过）。差别在**请求的发起者** —— 站点把 WebClient 模块的会话上下文绑在页面里（SPA 跑完 `SystemManager/GetSystemInitStatus` → `WebClient/GetCurrentUser` 之后才成立），非浏览器上下文的 fetch 不参与这套绑定，且返回码仍是 200，极难从响应上看出问题。现新增 `ensureCtxWin()`：用一个**不显示、不进任务栏**的 `BrowserWindow`（`persist:mc-query` 分区、`backgroundThrottling: false`）加载 `/index.html` 作**常驻页面宿主**，所有 WebCore / Preview 请求都改用 `executeJavaScript` 在该页面里 `fetch(...)`；窗口崩溃、eval 报 `Failed to fetch`、自有超时（`WJXT_PAGE_TIMEOUT`）时自动丢弃重建，登录成功后重新导航一次让页面用新会话重新绑定（＝上游对 404 的处置「重新导航首页」），应用退出时销毁；页面上下文起不来才降级回 `session.fetch`（`mainFetch`，日志标 `[main]` / `[fallback]`，该通道 0 条属预期）。日志行前缀可直接区分通道：`[page]` = 有效通道。⑯ 版本号 1.0.42 → 1.0.43，`buildVersion` 同步 1.0.43.0。⑰ **清理本地排查产物**：删除根目录下探测留下的 `__wjxt_*`（响应抓取 JSON、临时下载目录、上游参考包副本）与历史遗留的 0 字节文件 `1h`（v1.0.10 误入库），`.gitignore` 增加 `__*`（放行 `__tscheck.ps1`）与 `*.err` 兜底，避免这类一次性排查产物再进库。⑱ **修复「预览 / 下载」链接点错（点了下载得到一个没有后缀的文件）**：以前结果里只给一个 `previewUrl`，而渲染层那条「规格文件下载」判据 `/[?&]fileId=/i` **大小写不敏感**，把 `preview.html?fileid=<guid>` 也认成了 `fileId=` → 点「预览」直接弹「保存规格文件」对话框，默认文件名取自**链接文案**（「下载」）、保存类型「所有文件」，于是落盘一个**文件名错、没有后缀**的文件。现在：① 渲染层**先单独分流鸿翼链接**（限定 `streamax.com` + `preview.html` + `fileid`），并把规格文件判据收紧为大小写敏感的 `fileId=`；② **预览 = 在应用内窗口打开站点预览页**（同一登录分区免登录，`openOaWindow`，不触发下载）；③ **下载 = 弹「另存为」**：技能新增 `downloadUrl`（`preview.html?fileid=<guid>&mcdl=1&name=<原始文件名>`），渲染层识别 `mcdl=1` 后调用新增 IPC `mc-wjxt-download`，主进程按 `fileGuid` 换出带签名 token 的 `GetOriginFile` **原始文件直链** → `downloadBufferViaSession` 下载 → 「另存为」默认名＝**真实文件名（含后缀）**，并清理非法字符 → 写盘（失败/取消都有明确回执）；④ 搜索结果每行同时带 `previewUrl` 与 `downloadUrl`，工具描述与 SKILL.md 的输出格式改为**固定列表格** `编号 / 文件名 / 大小 / 修改时间 / 目录 / 创建人 / 预览/下载`，最后一列固定写成 `[预览](previewUrl) / [下载](downloadUrl)`（两个都要给，避免再拿同一个链接当两用）。⑲ **全部下载改为「先选位置、边下边写」（浏览器行为）**：此前所有下载都是「先把整份内容读进内存 `Buffer`，再弹保存框 / 写盘」—— 点下载要等整份下完保存框才出现（20MB 的 PDF 也得等数秒，大文件更久），内存占用还与文件大小成正比。现在 `fileDownload.ts` 的下载核心换成**流式** `downloadToPath()`：① **先弹「另存为」**，选完位置立刻开始写 `xxx.part`（数据分块落盘，内存只占一个块），完成后改名，失败 / 取消 / 超限自动删掉半截文件；② 超时策略也改成浏览器式 —— 「拿到响应头」30 秒上限，正文阶段用**空闲超时**（默认 60 秒无新数据才失败），大文件慢慢下不会被打断（此前是整段 60 秒总超时）；③ 主干（node 直连的 OA 规格文件下载）补上与会话通道一致的「期望文件却收到 `text/html` → 判登录态失效」判据，避免把登录页当规格文件落盘（探针实测旧实现会照样落盘）；④ `OA_FILE_DOWNLOAD` 与 `mc-wjxt-download` 两个 IPC、以及 AI 的 `file_download` / `wjxt_download` 全部改走新核心，`downloadBuffer*` 三个「整份进内存」的旧函数一并删除；⑤ 新增下载进度事件（`mc-download-progress`，限流 ~7 次/秒），AI 回复里的下载链接与规格文件表会显示「下载中 42%」。已验证：本地起慢速 HTTP 服务跑真实代码，30MB 边下边写的 `.part` 体积逐步增长（3→7→11→…→28MB）、字节数一致、`.part` 自动清理；HTML 拦截、重定向跟随、体积上限、用户中止、空闲超时下大文件下完，6 项探针全过。⑳ **修复鸿翼下载偶发「拿不到下载地址」（`WJXT_NO_FILE_URL`）+ 下载中光标变「圆圈禁行」**：① 前者是**会话「半建立」**形态 —— 分区里少了站点自己种的 `token` / `browserPlatform` 等 cookie（日志实测某次 `cookies=4`，正常为 7~8）时，`Preview/GetPreviewPara` 会回 HTTP 200 但**没有 `data.fileUrl`** 的小信封（约 400 字节），随后同一请求重试即成功；现在 `wjxtResolveOriginUrl` 会自动**重新导航隐藏窗口再取一次**，仍失败才报错，并把响应形态（status / 长度 / 带上的 cookie 数 / 正文前 300 字）写进 `wjxt.log`。② 下载流程改为**先换直链、再弹「另存为」**：取不到地址时直接报错，不再让用户先白选一次保存位置。③ AI 回复里的下载链接在下载中曾设 `aria-disabled`，而项目的自定义光标规则（`cursor.css` 引 `animal-island-ui`）对 `[aria-disabled='true']` 强制 `cursor: not-allowed`，光标会变成「圆圈禁行」、看着像卡住；现改用 `aria-busy`（防重复点击由 `.busy` 的 `pointer-events:none` 负责）。④ 鸿翼下载失败统一提示「下载失败：…」，`WJXT_NO_FILE_URL` 给出可执行说明（稍后重试 / 在应用内重新登录），不再套用「规格文件下载失败」的措辞。 |
