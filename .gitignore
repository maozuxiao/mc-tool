# 依赖：由 npm install 生成，依赖树已完整记录在 package-lock.json 中，
# 体积大（约 620 MB）且含平台特定二进制，不入库。
node_modules/

# 打包产物：由 build_now.ps1 / npm run pack:* 生成，
# 安装包通过 GitHub Releases 分发，无需入库。
dist/

# electron-vite 构建输出（中间产物）
out/

# 临时打包目录：build_now.ps1 / 手动 electron-builder --config electron-builder-test.yml 产生
dist-test*/

# 历史版本安装包归档：build_now.ps1 打包前会清空 dist/，
# 归档到此处（如 dist-archive/1.0.27/）便于随时回滚重装。
# 体积大，不入库；回滚锚点以 git 标签 v1.0.27 与 backup/v1.0.27 分支为准。
dist-archive/

*.log
.DS_Store

# Python 缓存：内置 skill 的查询脚本由 Node 调用，__pycache__ 是历史残留，无需入库
__pycache__/
*.pyc

# 图标：由 electron-builder 从 build/icon.png 自动生成，体积较大
build/icon.ico
build/icon.icns
