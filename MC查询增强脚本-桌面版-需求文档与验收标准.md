# MC 物料查询增强脚本（桌面版）需求文档与验收标准

- **文档版本**：v1.0
- **技术形态**：Electron 桌面应用
- **UI 设计规范**：animal-island-ui（Animal Crossing 风格组件库，React + TS，CC BY-NC 4.0）
- **文档用途**：作为开发 / AI 编码工具的实施依据，以及最终交付验收的checklist
- **读者**：产品/需求方、开发人员或负责实施的 AI 工具

> ⚠️ 本文档基于你在对话中提供的产品介绍文字撰写。原有半成品代码（user.js）与现状截图两次上传均未成功（文件为 0 字节 / 图片链接域名不在可访问白名单内）。因此文档中**功能需求**部分完全基于你给出的文字描述整理，**UI 规范**部分完全基于 animal-island-ui skill 包内容整理；文档末尾列出了需要你确认的假设与待澄清问题。建议后续把现有代码/截图直接以文件形式重新上传，我可以据此再做一版"现状 vs 目标"差异表。

---

## 1. 产品概述

### 1.1 背景
MC 物料查询插件原本内嵌于 OA 系统页面，用于快速查询物料料号、BOM、规格文件等信息。当前需求是把它做成一个**独立的 Electron 桌面应用**，脱离 OA 页面的 iframe/插件限制，同时应用 UI 需要迁移到 animal-island-ui 风格（温暖大地色系、胶囊圆角、3D 游戏按钮阴影等）。

### 1.2 目标用户
物料/采购/PMC/工程相关岗位人员，日常需要频繁查料号、查 BOM、导出结果做二次处理。

### 1.3 核心价值
- 减少在 OA 系统内多次跳转查询的操作成本
- 支持批量、模糊、多条件组合查询，一次性覆盖日常查料的绝大多数场景
- 结果可二次筛选、排序、去重后导出，直接用于下游 Excel/系统导入

---

## 2. 术语表

| 术语 | 说明 |
| --- | --- |
| 料号 | 物料编码，唯一标识一个物料 |
| BOM | Bill of Materials，物料清单，描述某料号由哪些子件构成，通常为树状层级结构 |
| 模糊匹配 | 输入内容作为子串/关键词匹配料号或描述，返回多条候选 |
| 精确匹配 | 输入完整料号，返回唯一或极少数结果 |
| 组合查询 (`&&`) | 多个关键词用 `&&` 连接，结果需同时包含所有关键词（AND 逻辑） |
| 去重 | 对合并后的结果按料号（或指定字段）去除重复行 |

---

## 3. 功能需求（Functional Requirements）

每项功能需包含：**输入 → 处理逻辑 → 输出 → 异常/边界情况**。

### FR-1 料号搜索
- **输入**：单个料号，允许完整或部分输入。
- **模式**：支持"模糊匹配"与"精确匹配"两种模式，用户可切换（建议用 `Radio` 或顶部 `Tabs` 区分，而非下拉，因为是高频切换的二选一操作）。
- **输出**：结果列表（表格形式），至少包含料号、描述、规格、单位等基础字段（字段清单需与现有 OA 数据源对齐，见 §8 待澄清问题）。
- **异常**：
  - 输入为空 → 禁用查询按钮，不触发请求。
  - 无匹配结果 → 表格显示统一空状态文案（对应 `Table` 的 `emptyText`），而不是空白页面。
  - 查询失败（网络/接口错误）→ 用 `Notification` 提示错误原因，不清空已有结果。

### FR-2 批量查询
- **输入**：一次性粘贴多个料号（换行或常见分隔符如逗号、空格分隔，需明确并在输入框 placeholder 中提示）。
- **处理**：逐个料号发起查询（或调用后端批量接口，视现有数据源能力而定），**合并**所有结果为一张结果表。
- **输出**：
  - 合并后的结果表格；
  - 对于"查询不到"的料号，需要有明确反馈（例如结果表中该行标记为"未找到"，用 `Tag` 标出，而不是静默丢弃）——这是当前最容易被漏做的点，务必作为验收重点。
- **异常**：
  - 粘贴内容中含空行/重复料号 → 自动清洗（去空、去重）后再查询，避免重复请求同一料号。
  - 数量过多（如 >200 条）→ 需要有查询中的进度反馈（`Progress` 或 `Loading`），并考虑分批请求防止阻塞界面。

### FR-3 多条件描述搜索
- **输入**：多个关键词，通过 `&&` 拼接（例如 `不锈钢&&M6&&螺丝`）。
- **处理**：按 AND 逻辑对物料描述字段做多关键词匹配，返回同时包含全部关键词的物料。
- **输出**：结果表格，命中的关键词建议在描述文本中高亮（可选加分项，非必须）。
- **异常**：
  - 关键词为空片段（如 `A&&&&B`）→ 自动忽略空片段，不报错。
  - 未输入 `&&` 时按单关键词模糊搜索处理（与 FR-1 模糊模式行为一致，避免用户困惑两套输入框）。

### FR-4 BOM 查询
- **输入**：单个料号。
- **输出**：该料号的 BOM 树（父子层级关系），需要能够展开/折叠子级。
- **实现约束**：animal-island-ui 的 `Table` 组件**不支持**内置的 `expandable`/嵌套行（见组件参考"Not supported"），因此 BOM 树**不能**依赖 `Table` 的展开功能实现，需要用如下方式之一自行实现层级展示：
  - 使用 `Collapse` 组件逐级展开每个节点（简单场景推荐）；
  - 或自定义树形结构组件，但视觉上必须遵守 animal-island-ui 的圆角/配色/间距 token，不能引入无关的第三方树组件默认样式。
- **异常**：料号无 BOM 数据 → 明确空状态提示，而不是空白树。

### FR-5 规格文件查询
- **输入**：单个料号。
- **输出**：该料号关联的规格文件/附件列表（文件名、类型、大小、可能的预览或下载入口）。
- **异常**：无关联文件 → 空状态提示；点击下载失败 → `Notification` 报错。

### FR-6 结果筛选、排序、去重
- **前提**：animal-island-ui 的 `Table` **不内置** `sorter`/`filters`/分页/`rowSelection`，这些都需要在应用层自行实现状态管理，再把处理后的数组传给 `Table` 的 `dataSource`。这是本次实施中最容易"功能缺失"的部分，需要作为独立开发任务明确排期，而不是想当然认为组件自带。
- **筛选**：至少支持按字段值筛选（如按料号/描述关键词二次过滤当前结果集，不重新发请求）。
- **排序**：至少支持按某一列（如料号、描述）升/降序排序，点击表头切换。
- **去重**：按料号去重，用户可选择开启/关闭（`Switch` 控制）。
- **组合行为**：筛选、排序、去重三者可叠加使用，操作顺序不影响最终结果的正确性。

### FR-7 CSV 导出
- **范围**：支持导出"物料查询结果"和"BOM 查询结果"两类数据（各自独立导出，字段结构不同）。
- **导出内容**：应导出**当前经过筛选/排序/去重处理后**的结果，而不是原始未处理数据（除非产品另有约定，需与需求方确认）。
- **文件命名**：建议包含查询类型 + 时间戳，避免覆盖历史导出文件。
- **编码**：CSV 需正确处理中文（建议 UTF-8 with BOM，避免 Excel 打开乱码）。
- **异常**：结果为空时导出按钮禁用或提示"无可导出数据"。

### FR-8 中英文双语
- **范围**：界面全部文案（按钮、标签、表头、空状态、错误提示、Tooltip 等）均需支持双语，不能只切换部分文案。
- **切换方式**：使用 `Select`（受控组件，`options`+`value`+`onChange` 三者必须齐全）或语言 `Switch`，切换后立即生效，无需重启应用。
- **持久化**：语言选择应在应用重启后保持（本地存储，Electron 场景可用本地配置文件/`electron-store` 等，具体方案由实现方决定）。

### FR-9 全局导航
- 根据以上功能分组，顶层需要有清晰的模块切换入口。使用 `Tabs` 组件（`items` + `activeKey`/`defaultActiveKey`）划分：
  1. 料号搜索（含批量查询，可作为该 Tab 内的子模式切换，而非单独顶层 Tab，避免 Tab 过多）
  2. 描述搜索
  3. BOM 查询
  4. 规格文件查询
- 是否需要独立"历史记录/收藏"模块，需与需求方确认（见 §8）。

---

## 4. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 单次查询（含批量 ≤200 条）应在合理时间内给出可感知的加载反馈（`Loading`/`Skeleton`），不能出现界面卡死无响应 |
| 错误处理 | 所有网络/接口异常必须有用户可见的反馈（`Notification`），不能静默失败或仅在控制台打印 |
| 空状态 | 所有列表/表格类展示都必须有明确的空状态文案，且双语 |
| 本地缓存 | 建议对最近一次查询结果做本地缓存，应用重启不丢失（可选，需确认优先级） |
| 桌面应用规范 | 需有正常的窗口标题、图标、菜单（或至少最小化/关闭按钮），符合 Electron 应用基本规范 |
| 数据安全 | 若涉及公司内部物料数据，需确认查询接口的鉴权方式（复用 OA 登录态？独立账号？）——见 §8 |

---

## 5. UI 规范映射表（功能 → animal-island-ui 组件）

> 以下映射是本次验收的核心依据。凡是页面上出现的可交互元素，都必须能在此表中找到对应关系；找不到对应关系的自造样式，视为"未按 skill 实施"。

| 界面元素/场景 | 对应组件 | 关键约束 |
| --- | --- | --- |
| 顶部模块切换（料号搜索/描述搜索/BOM/规格文件） | `Tabs` | `items` 必填；建议受控模式（`activeKey`+`onChange`）便于保留状态 |
| 料号输入框、批量粘贴框、关键词输入框 | `Input` | 50px 胶囊圆角；批量粘贴框建议用 `textarea` 风格但仍需遵循 token（若库内无原生 textarea，需自定义但套用 `--animal-*` 变量，不可用默认浏览器样式） |
| 模糊/精确模式切换 | `Radio` | 二选一场景用 `Radio`，而非 `Select` 下拉 |
| 查询/导出/重置等操作按钮 | `Button` | 主操作用 `type="primary"`（自带 3D 像素堆叠阴影）；次要操作用 `default`/`text`；危险操作（如清空）用 `danger` |
| 查询中状态 | `Loading` 或 `Button` 的 `loading` 属性 | 全屏/区域级用 `Loading`；按钮自身处理中用 `loading` 属性（会显示斜纹动画），二者不要同时滥用 |
| 表格骨架屏（结果加载中的过渡） | `Skeleton` | 可选，用于结果表格首次加载前的占位 |
| 查询结果表格 / BOM 展示 | `Table` | 注意：无内置分页/排序/筛选/展开，见 §3 FR-6、FR-4 |
| "未找到"/状态标记 | `Tag` | 如批量查询中未命中的料号，用 `Tag color="app-red"` 或 `variant="outlined"` 标出，而非纯文字 |
| 语言切换 | `Select`（受控） | `options`+`value`+`onChange` 三者必须齐全，无 `defaultValue` |
| 结果去重开关 | `Switch` | — |
| BOM 树层级展开 | `Collapse`（逐级）或自定义树 | 不能用 `Table` 的展开行为，库不支持 |
| 规格文件预览/确认类弹窗 | `Modal` | 必须保留 SVG blob 裁剪的有机形状，不能改成圆角矩形 |
| 侧边详情/筛选面板（如有） | `Drawer` | 若产品设计需要滑出面板承载"筛选/排序"控件，可用 `Drawer` |
| 操作提示（悬浮说明） | `Tooltip` | — |
| 各类图标（搜索、导出、清空等） | `Icon` | 仅限库内置 10 个 `IconName`，不可用 emoji、Unicode 符号或第三方图标 |
| 分组卡片（如结果统计卡） | `Card` | Card 无阴影，颜色从 13 个预设色中选取 |
| 章节标题（如"BOM 查询结果"） | `Title` | 燕尾丝带样式；不可用普通 `<h2>` 或 `Card type="title"`（该用法已不存在） |
| 分隔线 | `Divider` | 固定高度 12px，纯装饰 |
| 操作成功/失败提示 | `Notification`（命令式 API） | — |

### 5.1 硬性规则对照（来自 SKILL.md，共 14 条）

实施与验收时逐条对照，任何一条不满足都判定为"未按 skill 实施"：

1. 不允许出现组件参考文档之外的自造 props
2. 应用入口必须且只能引入一次 `import 'animal-island-ui/style'`
3. 不允许纯黑文字（`#000`/`#111`）或冷灰背景（`#fafafa`/`#f5f5f5`）
4. 不允许冷色调蓝色 focus 环；焦点色只能是黄色（输入框）或薄荷绿（按钮）
5. 所有可交互元素圆角不得小于 12px；按钮/输入框为 50px 胶囊
6. 3D 像素堆叠阴影只属于 primary/danger-primary 按钮；`Card` 无阴影；`Switch` 无外阴影；`Input` 阴影需显式开启
7. `Modal` 必须保留 SVG blob 裁剪路径；`Title` 必须是燕尾丝带，不能是圆角块
8. 字体为 Nunito + Noto Sans SC，字重不低于 400，UI 文本不使用等宽字体（`CodeBlock` 除外）
9. 动效统一使用 `cubic-bezier(0.4, 0, 0.2, 1)`，时长 0.15–0.35s
10. 图标必须来自 `<Icon name="..." />`（10 个内置名称），禁止 emoji/Unicode 符号/手写 SVG/第三方图标字体
11. `Select` 必须受控（`options`+`value`+`onChange` 齐全）；`Input`/`Switch`/`Checkbox`/`Radio` 受控时也必须提供 `onChange`
12. 优先使用库组件而非原生 HTML：页面上不应出现裸露的原生 `<button>`、`<input>`、`<select>`、checkbox/radio
13. 只从包根路径和 `animal-island-ui/style` 导入，禁止深层导入
14. 自定义元素样式必须使用 `var(--animal-*)` token，禁止用 `className`/`style` 覆盖组件自身的颜色/圆角/阴影

---

## 6. 关键用户旅程（供联调/测试参考）

1. **单个料号精确查询**：进入"料号搜索" Tab → 切换到"精确匹配" → 输入完整料号 → 点击查询（按钮进入 `loading` 态）→ 结果表格展示单条记录 → 可选导出 CSV。
2. **批量查询**：进入"料号搜索" Tab → 切换到批量模式 → 粘贴多行料号 → 点击查询 → 显示查询进度 → 结果表格展示合并结果，未命中料号用 `Tag` 标出 → 可对结果排序/去重后导出。
3. **多条件描述搜索**：进入"描述搜索" Tab → 输入 `关键词1&&关键词2` → 查询 → 结果表格展示同时命中两个关键词的物料。
4. **BOM 查询**：进入"BOM 查询" Tab → 输入料号 → 查询 → 以可展开层级形式展示 BOM 树 → 可导出 BOM 结果为 CSV。
5. **语言切换**：任意页面切换语言下拉 → 全部界面文案（含空状态、Tag、按钮、表头）立即变为目标语言，无需刷新。

---

## 7. 验收标准（Acceptance Criteria）

### 7.1 功能验收

- [ ] 料号搜索支持模糊与精确两种模式，且可明确切换
- [ ] 批量查询支持粘贴多个料号，自动去空/去重后逐个查询并合并展示
- [ ] 批量查询中"未查到"的料号有明确视觉标记，不会被静默丢弃
- [ ] 多条件描述搜索支持 `&&` 组合，AND 逻辑正确（可用至少 3 组测试用例验证）
- [ ] BOM 查询能展示层级树结构，可展开/折叠
- [ ] 规格文件查询能列出关联附件，支持查看/下载
- [ ] 结果表格支持筛选、排序、去重，且三者可叠加使用不冲突
- [ ] CSV 导出功能对"物料结果"和"BOM 结果"分别可用，中文不乱码
- [ ] 导出的数据是当前筛选/排序/去重后的结果（而非原始全量，除非另有约定）
- [ ] 中英文双语覆盖全部界面文案，包括空状态、错误提示、Tag 文案
- [ ] 语言切换即时生效，且重启应用后保持上次选择
- [ ] 所有查询失败场景都有用户可见的错误提示，不是控制台静默报错
- [ ] 所有空结果场景都有统一的空状态文案

### 7.2 UI 合规验收（逐条对照 §5.1 的 14 条硬性规则）

- [ ] 未发现自造/超出组件文档范围的 props
- [ ] 全局样式表只在入口引入一次
- [ ] 界面中不存在纯黑文字或冷灰背景
- [ ] 所有 focus 状态为黄色或薄荷绿，没有冷蓝色
- [ ] 所有按钮、输入框为 50px 胶囊圆角，其余可交互元素圆角 ≥12px
- [ ] 3D 阴影只出现在 primary/danger-primary 按钮上，Card/Switch 无对应阴影
- [ ] 弹窗（如有）保留 SVG 有机裁剪形状，标题使用燕尾丝带 `Title`（如有）
- [ ] 字体为 Nunito + Noto Sans SC，无小于 400 的字重，UI 文本无等宽字体
- [ ] 动效时长与缓动曲线符合规范（0.15–0.35s，`cubic-bezier(0.4,0,0.2,1)`）
- [ ] 所有图标均通过 `<Icon name="..." />` 渲染，未出现 emoji/Unicode 符号/自制 SVG/第三方图标库
- [ ] `Select` 为受控组件，三要素齐全；其余受控表单组件均有 `onChange`
- [ ] 页面无裸露原生 `<button>`/`<input>`/`<select>`/原生 checkbox、radio
- [ ] 导入路径均为包根或 `/style`，无深层导入
- [ ] 自定义样式全部使用 `var(--animal-*)`，未硬编码颜色覆盖组件默认视觉

### 7.3 非功能验收

- [ ] 批量查询 200 条以内有明确加载中反馈，界面不卡死
- [ ] 应用重启后语言/上次查询偏好（如需要）能正确恢复
- [ ] Electron 应用有正常的窗口标题/图标/基本窗口控制
- [ ] 所有异常路径（网络失败、接口报错、数据为空）均有对应文案，且双语

---

## 8. 待确认问题（需求方需回答，否则以下假设生效）

| # | 问题 | 当前假设（若不回复将按此执行） |
| --- | --- | --- |
| 1 | 查询结果具体需要展示哪些字段（料号/描述/规格/单位/库存/供应商...） | 按最基础的"料号+描述+规格+单位"实现，其余字段待现有接口返回数据确定 |
| 2 | 批量查询上限（多少条算过多，需要分批/限流） | 暂定 200 条为分批阈值 |
| 3 | 导出 CSV 是导出"处理后结果"还是"原始全量结果" | 假设导出处理后（筛选/排序/去重后）的结果 |
| 4 | 是否需要历史记录/收藏功能 | 暂不纳入本期范围 |
| 5 | 数据接口鉴权方式（复用 OA 登录态 / 独立 token） | 待确认，暂不在本文档内约定实现方案 |
| 6 | 本地缓存/离线可用性要求 | 暂定为可选加分项，非本期强制要求 |

---

## 9. 建议的验收流程

1. 先过 **§7.2 UI 合规验收**（相对客观、可逐条打钩，最容易发现"没按 skill 实施"的问题）。
2. 再过 **§7.1 功能验收**，建议对每条用真实数据跑一遍关键用户旅程（§6）。
3. 最后过 **§7.3 非功能验收**。
4. 对未通过项，直接引用本文档对应编号（如 "FR-6 排序未实现" / "规则 5 按钮圆角不足"）反馈给开发/AI 工具，减少沟通成本。

---

如果你能重新上传现有的 user.js 代码和实际截图，我可以在这份文档基础上再产出一份**"现状代码 vs 本文档"的逐项差异表**，直接标出哪些代码需要改、怎么改。
